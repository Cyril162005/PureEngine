{
  "project": "PureEngine - Game Build",
  "depends_on_project": "PureEngine (12/12 complete, all steps verified and pushed)",
  "game_identity": {
    "title": "TBD",
    "genre": "Top-down arena survival",
    "core_loop": "Avoid/outlast hostile entities in a bounded arena; survive as long as possible",
    "v1_status": "Shipped in engine Step 12 - one player, one hostile, AABB catch detection, survival timer (console only), MENU/PLAYING/PAUSED/GAME_OVER state machine"
  },
  "phases": [
    {
      "id": 1,
      "name": "Multiple Hostiles",
      "status": "completed",
      "definition_of_done": "2-3+ hostiles simultaneously chase the player, added as entity-vector data (push_back), reusing existing update/draw/collision loops with zero new code paths",
      "notes": "Three hostiles total: two push_backs before the initialEntities snapshot (spawn points (3,2) CW spin and (-3,2) faster CCW spin beside the original (0,-2)), parallel const float hostileSpeeds[] {1.8, 1.6, 1.5} mapped by index h-3 — all slower than the player's 2.5 so escape stays possible (Step 12 principle). Chase generalized to a loop over indices 3..N-1 using the same four Step 5 Vec3 ops per hostile; catch test generalized to a loop OR-ing Step 8's aabbOverlap into one bool — ANY hostile ends the run through the identical Step 12 death block. Scenery pair loop bound untouched; resetGame() needed ZERO changes (snapshot/restore absorbs the new entities). CMakeLists.txt and gamestate.h unchanged (pure logic, no new states). Fresh clean rebuild passed first try, zero engine warnings. Verified by user: independent tracking at varied speeds, scenery/each-other pass-through, identical lose flow, all six entities freeze on pause, full reset correct.",
      "depends_on_engine_steps": [7, 8, 12]
    },
    {
      "id": 2,
      "name": "Difficulty Scaling",
      "status": "not_started",
      "definition_of_done": "Hostile speed and/or spawn rate increases with survivalTime, using deltaTime-driven state rather than fixed constants",
      "depends_on_engine_steps": [2, 12],
      "depends_on_phase": [1]
    },
    {
      "id": 3,
      "name": "On-Screen Text (Score/Timer Display)",
      "status": "not_started",
      "definition_of_done": "Survival time visible on screen during PLAYING and on GAME_OVER, not console-only. First new engine capability required post-Step-12 (text/font rendering).",
      "depends_on_engine_steps": [11, 12]
    },
    {
      "id": 4,
      "name": "Scoring Beyond Survival Time",
      "status": "not_started",
      "definition_of_done": "Either a defined additional scoring mechanic is implemented and displayed, or a deliberate decision is recorded that survival time alone is sufficient",
      "depends_on_phase": [3]
    },
    {
      "id": 5,
      "name": "Content Pass",
      "status": "not_started",
      "definition_of_done": "Player, hostiles, and arena visually distinct from each other and from Step 4-10 placeholder checkerboard geometry",
      "depends_on_engine_steps": [10]
    },
    {
      "id": 6,
      "name": "Packaging",
      "status": "not_started",
      "definition_of_done": "Exe + assets runnable on a clean machine without dev environment installed, or a documented clean-checkout build process",
      "depends_on_phase": [1, 2, 3]
    }
  ],
  "kill_criteria": "If any phase's scope keeps expanding instead of shrinking, stop, cut scope, and re-record a smaller definition_of_done before continuing."
}
