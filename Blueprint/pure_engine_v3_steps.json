{
 "project": "PureEngine",
 "method": "from_scratch",
 "hardware_target": "i3, 8GB RAM",
 "stack": [
  "C/C++",
  "GLFW or SDL",
  "OpenGL"
 ],
 "continuation_of": "pure_engine_v2_steps.json (Steps 25-34 â€” engine hardening: data-driven config, second scene, frame-time visibility, automated tests)",
 "status_note": "This continuation tracker covers Steps 35 through 153 (Steps 25-34 remain in pure_engine_v2_steps.json). Steps 35 through 50 covered engine hardening continuation, data-driven hostile config, alternate scenes, depth sorting, entity roles, and stress-test validation; v1.0 froze at Step 100 and Steps 101+ are an additive Phase 2 continuation.",
 "steps": [
  {
   "id": 35,
   "name": "Wire existing beep to new-high-score event",
   "status": "completed",
   "definition_of_done": "The existing single-beep round-robin fires when a run beats the stored high score, without adding sound assets or changing the audio API.",
   "notes": "This is the smallest honest use of the current one-asset audio system. Collision-edge and GAME_OVER events already trigger the current beep through playNext(); the high-score branch now uses that same mechanism at the real record-break moment.",
   "implementation": "In src/main.cpp, the high-score branch calls audio.playNext() before updating the in-memory score and saving it. No new asset, no new audio API, no sound-type abstraction was introduced.",
   "verification": "Verified by Cyril: build succeeded, a full run reached 53.5884s and beat the prior high score of 47.7s; the console printed NEW HIGH SCORE and the beep was audible at that moment, alongside the existing catch/GAME_OVER triggers.",
   "depends_on": [
    20
   ]
  },
  {
   "id": 36,
   "name": "Distinct event audio (game-over, high-score) + bug fixes",
   "status": "completed",
   "definition_of_done": "The project ships the runtime asset copy fix, the silent loaded-flag bug fix, and the one-sound-per-death UX rule: record death plays only win_sound.wav; normal death plays only GAMEOVER.wav. The tracker records the remaining known follow-up about the long GAMEOVER clip.",
   "notes": "Packaging bug fixed: CMake now copies GAMEOVER.wav and win_sound.wav next to the executable, so the runtime path matches the game's 3-candidate asset probe. Silent-flag bug fixed: gameOverSoundLoaded and newHighScoreSoundLoaded were never set true after successful loadNamedSound() calls, so both sounds loaded but were immediately rejected by the playback guards. UX fix: in src/main.cpp, the caught-death branch chooses a single event sound per death, prioritizing the record-break cue over the normal death cue. Known follow-up, not blocking: GAMEOVER.wav is approximately 8.6 seconds long, longer than ideal for a game where deaths are sub-second; a shorter ~2-3s replacement clip is planned later but not yet implemented.",
   "implementation": "In src/audio.h, set the event-loaded flags to true immediately after each successful loadNamedSound() call. In src/main.cpp, the caught-death block now only triggers win_sound.wav on a record-break and only triggers GAMEOVER.wav on a normal death. CMakeLists.txt copies both event asset files into the build output assets/ directory alongside the existing beep.wav and hostile_default.txt bundle.",
   "verification": "Verified by build and runtime checks: PureEngine.exe rebuilt successfully, runtime logs showed GAME OVER and NEW HIGH SCORE events firing correctly, and the final run confirmed a record death played only win_sound.wav while a non-record death played only GAMEOVER.wav. The long clip length is noted as follow-up work rather than a blocker.",
   "depends_on": [
    20,
    35
   ]
  },
  {
   "id": 37,
   "name": "Collision hit-flash feedback",
   "status": "completed",
   "definition_of_done": "A brief collision hit pulse is synchronized with the existing collision sound using the current render path, and the tracker records the investigated-but-deferred collision-perception quirk that the rotation-safe AABB is larger than the visible triangle sprite.",
   "notes": "The flash uses the same edge event that already triggers the collision beep: when any new collision is detected, collisionFlashFrames = 6 and the render block overrides the active clear color for a few frames. Visual confirmation: the pulse reads as a quick hit-pulse, not a sustained tint. Investigated but deferred: the collision check itself is correct and loops player vs each hostile individually; the apparent bug is more likely a visual-perception mismatch because the rotation-safe hitbox is a 1.414x1.414 AABB while the visible triangle sprite is roughly 1.0x1.0. This remains a non-blocking known quirk for later debug-overlay work, not a gameplay bug to fix in this pass.",
   "implementation": "In src/main.cpp, the anyNewCollision branch sets collisionFlashFrames = 6 and reuses the existing clear color override in the render block so the flash lands on the same frame as the collision audio cue. No new abstraction or subsystem was introduced.",
   "verification": "Verified by runtime confirmation: the flash reads as a quick hit-pulse instead of a lingering tint. The collision investigation was reviewed from source: player-vs-hostile overlap checks are per-hostile loops, and the mismatch is a deferred visual quirk rather than a logic bug to fix now.",
   "depends_on": [
    20,
    35,
    36
   ]
  },
  {
   "id": 38,
   "name": "Visually distinct hostile types via textureId",
   "status": "completed",
   "definition_of_done": "Hostile definitions can select a texture variant through an optional textureId, the value is threaded through Entity into the renderer, and the alternate scene visibly demonstrates textureId=1 while legacy/default data remains unchanged.",
   "notes": "The first implementation pass exposed a missing data link: adding texture selection only to renderer.h was insufficient because Entity did not carry textureId. The completed chain is HostileDefinition -> lifecycle.h Entity construction -> Entity.textureId -> renderer.h per-hostile texture selection. The new assets/tex_hostile_alt.png is a 16x16 RGB texture matching tex_hostile.png's format, and assets/hostile_alt.txt uses textureId=1 for one hostile while retaining the optional 5th hostile value for backward compatibility.",
   "implementation": "In src/hostile_data.h, HostileDefinition gained textureId defaulting to 0 and hostile= accepts either four legacy values or an optional fifth texture id. In src/entity.h, Entity gained textureId defaulting to 0; src/lifecycle.h threads each HostileDefinition.textureId into the hostile Entity. In src/renderer.h, tex_hostile_alt.png is loaded, managed, and selected for entities whose textureId is 1. assets/hostile_alt.txt demonstrates the new format.",
   "verification": "Verified by Cyril with a real visual test: pressing 2 from the menu loaded hostile_alt.txt and displayed one orange hostile using tex_hostile_alt.png alongside two normal crimson hostiles. Pressing SPACE loaded the default scene, where all hostiles remained crimson with no regression.",
   "depends_on": [
    20,
    36,
    37
   ]
  },
  {
   "id": 39,
   "name": "Win condition via configurable win_time",
   "status": "completed",
   "definition_of_done": "The game has an independent configured win_time target, enters a frozen distinct WIN state after the catch check, preserves endurance scores beyond the difficulty cap, and avoids conflating a normal win with a new-high-score audio event.",
   "notes": "The win target is deliberately decoupled from maxDifficultyScale timing: both hostile scene files define win_time=120.0, so reaching the difficulty cap does not end the endurance run. GameState::WIN reuses GAME_OVER's frozen-world and SPACE-to-menu pattern with a distinct victory clear color. A UX bug was found and fixed: the high-score cue now plays in the WIN branch only when survivalTime > highScore as well; an ordinary win is silent because no dedicated win asset exists yet. A dedicated win sound remains future follow-up work, alongside the shorter GAMEOVER.wav replacement noted in Step 36."
  },
  {
   "id": 40,
   "name": "Distribution packaging (fixed asset bundle + package.ps1)",
   "status": "completed",
   "definition_of_done": "A clean Release build copies all eleven runtime assets automatically, and package.ps1 creates a correctly structured PureEngine-0.1.0-win64.zip containing only PureEngine.exe and the complete assets directory, with runtime save data excluded.",
   "notes": "The CMake POST_BUILD rule now copies the complete runtime asset bundle: checker.png, font_digits.png, tex_player.png, tex_scenery.png, tex_hostile.png, tex_hostile_alt.png, hostile_alt.txt, beep.wav, GAMEOVER.wav, win_sound.wav, and hostile_default.txt. scripts/package.ps1 stages the Release executable and explicit asset list into a clean PureEngine-0.1.0-win64 folder, then creates the zip; package/ is ignored because the archive and staging folder are build output.",
   "implementation": "CMakeLists.txt was extended with copy_if_different commands for the seven previously missing runtime assets. scripts/package.ps1 defines the single version variable $version = \"0.1.0\", recreates package/, copies only PureEngine.exe and the eleven runtime assets, validates each input, and compresses the correctly named staging folder. .gitignore excludes package/.",
   "verification": "Verified end-to-end by Cyril: a clean-state Release rebuild copied all eleven runtime assets; package.ps1 produced PureEngine-0.1.0-win64.zip; extraction produced the correct single folder depth; the extracted build ran standalone from a fresh location with no savedata carried over; multiple real sessions saved new high scores correctly.",
   "depends_on": [
    38,
    39
   ]
  },
  {
   "id": 41,
   "name": "Consolidate texture-loading duplication in renderer.h",
   "status": "completed",
   "definition_of_done": "The four repeated entity texture candidate-array blocks are consolidated behind loadRgbAsset() without changing candidate order, loading behavior, or init failure handling.",
   "notes": "The player, scenery, hostile, and hostile-alt texture loads previously each built the same three-candidate path array before calling loadRgbTexture(). The new loadRgbAsset() helper builds assets/, ../assets/, and ../../assets/ candidates internally and returns the synchronous loadRgbTexture() result, including 0 on failure.",
   "implementation": "In src/renderer.h, the four entity texture call sites now use loadRgbAsset() with their base filenames. The legacy checker path remains separate with its existing dedicated failure message; the combined entity-texture failure check is unchanged.",
   "verification": "Verified by Cyril: the Release build succeeded, both default and alternate scenes rendered their textures normally, no visual regression occurred, and the game did not crash. This was a pure refactor with no behavior change.",
   "depends_on": [
    38,
    40
   ]
  },
  {
   "id": 42,
   "name": "Debug hitbox overlay (F1) + fix untracked audio assets",
   "status": "completed",
   "definition_of_done": "Two real bugs fixed (dist-breaking untracked audio assets + orphan clutter PNG removed); a temporary F1-toggle debug AABB overlay added; the Step-37 collision-perception open question visually resolved (not a bug).",
   "notes": "Asset-tracking bug found via repo-verification process (not trusted tracker): assets/GAMEOVER.wav and assets/win_sound.wav were referenced by CMake POST_BUILD, audio.h, and package.ps1, but were NEVER git-added despite Step-36/40 tracker claims. Reproduction: fresh git clone + Release build FAILED because CMake POST_BUILD copy commands could not find the files. Fix: git add both + commit pushed as e8eb867, then re-verified by cloning to D:\\tmp-verify-clone from origin/main and rebuilding Release end-to-end â€” 11/11 assets copied into build\\Release\\assets\\ with exact byte counts. Separately, assets/game over.png (1024x1024 mockup) was deleted from disk as untracked dead weight â€” 0 code references, 0 CMake copies, 0 texture loads. Step-42 feature: F1 edge-toggle debug overlay added. renderer.h introduces a tiny static 4-vertex GL_LINE_LOOP unit square (aabbVAO/aabbVBO, 80 bytes) using the EXISTING interleaved 5-float attribute layout, same shader, same color/transform uniforms, no new shader sources, no new assets, no new uniforms. drawAABBs() draws each entity's NON-rotated, scale*halfExtents*2 rectangle â€” the EXACT value collision.h's aabbOverlap tests â€” with orange for player (i==0) and yellow for hostiles/scenery. main.cpp: Input ctor adds GLFW_KEY_F1, adds bool debugHitboxes=false at the collisionFlashFrames scope, toggles it on F1 edge in PLAYING/PLAYING_ALT, and conditionally calls drawAABBs() between drawWorld and HUD in all drawsWorld() states (both scenes). Runtime visual verification by Cyril: F1 shows a larger non-rotating axis-aligned square wireframe behind each rotating triangle sprite â€” visually confirming the Step-37 open question: the AABB rotation-safe hitbox (1.414x1.414 units) intentionally extends past the sprite edges. Resolved: the earlier 'catches in the gap, not on contact' perception was a sprite-vs-hitbox visual gap, NOT a logic bug. aabbOverlap() has been mathematically correct all along.",
   "implementation": "Step A (e8eb867): git add assets/GAMEOVER.wav assets/win_sound.wav, commit, push. Step B: fresh clone of origin/main to D:\\tmp-verify-clone confirmed all 11 asset files cloned, cmake -S. -Bbuild succeeded, cmake --build build --config Release succeeded, dir build\\Release\\assets\\ showed both wav files with correct sizes (GAMEOVER=3,025,034; win_sound=785,082). Step C: assets/game over.png deleted from working tree (was untracked, no commit needed, git index never knew it existed). Step D: src/renderer.h gains 4 members (aabbVAO/aabbVBO handles, destroyAll cleanup, 4-vertex GL_LINE_LOOP VAO/VBO init) + drawAABBs(projection,view,entities) public method reusing shaderProgram, transformLocation, colorLocation, checkerTexture. src/main.cpp: Input{..., GLFW_KEY_F1}, bool debugHitboxes=false, F1 edge toggle in PLAYING/PLAYING_ALT, conditional drawAABBs() call after drawWorld before HUD.",
   "verification": "Verified with real command output. (1) Git-state: before fix, git ls-files assets/ had 9 tracked files only; after e8eb867, both wav files tracked. (2) Fresh-clone build: clone â†’ cmake configure â†’ Release build exit 0, PureEngine.exe + hostile_data_test.exe produced, all 11 files copied to build/Release/assets/. (3) src changes: git diff confirmed exactly 8 hunks â€” +77/-0 renderer.h, +15/-1 main.cpp. (4) Release rebuild: exit 0, zero warnings. (5) Runtime visual test by Cyril: F1 toggled square wireframes drawn per entity, non-rotating (axis-aligned while triangle sprite spins), visibly larger than the sprite. Step-37 open question resolved: perception mismatch, not bug.",
   "depends_on": [
    37,
    41
   ]
  },
  {
   "id": 43,
   "name": "Shorten GAMEOVER.wav clip (8.6s -> 2.5s)",
   "status": "completed",
   "definition_of_done": "Original 8.6s GAMEOVER.wav (3,025,034 bytes, float32 stereo 44.1kHz) replaced with a 2.5s version under the SAME filename so 0 code references change; build deploys the new blob via the existing CMake copy rule; runtime death event plays the new noticeably shorter clip with smooth fade.",
   "notes": "Known follow-up since Step 36: original GAMEOVER.wav was 8.6 seconds long, absurd for sub-second arcade deaths where a fresh run starts with SPACE immediately. Replaced via Scenario B (same filename, pure binary blob swap) so zero code or config files need string edits. Expected references (audited pre-swap via grep) were 3 production-code sites: audio.h:138 loadNamedSound string, CMakeLists.txt:103-104 POST_BUILD copy_if_different src/dst, scripts/package.ps1:13 asset-bundle list item. All three retained unchanged because filename stayed 'GAMEOVER.wav'. Expected byte size for 2.5s float32 stereo 44.1kHz: ~882,044 bytes plus header/metadata; actual on-disk size after swap = 887,824 bytes (within ~5.8KB overhead of theoretical size â€” confirms ~2.5s at the claimed format). Build-time copy_if_different propagated the 887,824-byte blob into build/Release/assets/GAMEOVER.wav with a post-build timestamp, replacing the old 3,025,034-byte file â€” verified by dir listing before and after rebuild. Runtime sound quality (smooth fade vs abrupt cut) and perceived duration difference require actual speaker playback from a user session, which a headless build agent cannot verify.",
   "implementation": "Blob-only swap: assets/GAMEOVER.wav bytes replaced in-place on disk at 9/4/2026 12:33 AM, timestamp changed from 9/2 12:45 AM (old), length dropped from 3,025,034 bytes to 887,824 bytes. Zero source files edited. Zero CMake strings edited. Zero package.ps1 list entries changed. CMake POST_BUILD's copy_if_different rule deployed the new blob automatically during the next Release rebuild because mtime/size differed from the build-output copy. Only non-binary artifact change: this Step 43 tracker entry.",
   "verification": "Verified with real command output: (1) dir assets/GAMEOVER.wav before swap = 3,025,034 bytes / 9/2; after swap = 887,824 bytes / 9/4 12:33 AM â€” confirmed. (2) git status assets/GAMEOVER.wav returned ' M' (tracked modified; not ??), proving the blob is a real tracked update to an already-indexed file â€” no new git add trap like Step 36's original bug. (3) cmake --build build --config Release exit 0. (4) dir build/Release/assets/GAMEOVER.wav after rebuild = 887,824 bytes / 9/4 12:36 AM (freshly copied, mtime matches build run) â€” POST_BUILD copy rule worked. (5) Runtime ear-test confirmation by Cyril: normal-death death event plays the noticeably shortened ~2.5s clip with a smooth fade-out and no abrupt cut. Perceived length matches the 8.6s -> 2.5s intent.",
   "depends_on": [
    36,
    42
   ]
  },
  {
   "id": 44,
   "name": "Attempt to stop event audio on menu return (superseded by Step 48)",
   "status": "superseded",
   "definition_of_done": "Event audio (GAMEOVER.wav / win_sound.wav) stops when returning to MENU from GAME_OVER or WIN.",
   "notes": "original attempt to fix event-audio-bleed via stopEventSounds(), failed for reasons never root-caused, superseded by Step 48's corrected implementation. This closes the 1-50 sequence gap.",
   "implementation": "No source code changes were committed for this step. The attempt is recorded only as a known issue note in Blueprint/PURE_ENGINE_V3.md (commit 1c536a2).",
   "verification": "Commit 1c536a2 documents the failure. Step 48 later verified the fix with runtime testing.",
   "commit": "1c536a2",
   "depends_on": [
    36
   ]
  },
  {
   "id": 45,
   "name": "Entity depth field + stress-test baseline at 3/20/50 hostiles",
   "status": "completed",
   "definition_of_done": "Entity carries an explicit depth field, and a stress test at 3, 20, and 50 hostiles establishes the baseline frame-time curve for future collision and rendering comparisons.",
   "notes": "The depth field makes draw-order intent explicit in data. The stress test used data-driven hostile_default.txt variants; average frame time stayed flat at ~1.5-1.6 ms across all three counts. Re-verified post-Steps 46-49 at 50 hostiles: 1.47-1.80 ms (avg ~1.6 ms), confirming no performance pressure.",
   "implementation": "In src/entity.h, Entity gained an int depth member defaulting to 0. In src/lifecycle.h, buildInitialEntities assigns depth 0=player, 1=scenery, 2=hostile. The stress-test asset files (hostile_stress_50.txt etc.) were added to assets/.",
   "verification": "Verified by runtime measurement: 3, 20, and 50 hostiles all produced flat ~1.5-1.6 ms avg frame time. Re-verified post-Steps 46-49 at 50 hostiles: 1.47-1.80 ms (avg ~1.6 ms), commit 08f7d98.",
   "commit": "e1bee39",
   "depends_on": [
    34
   ]
  },
  {
   "id": 46,
   "name": "Parameterize scanSceneryCollisions sceneryCount",
   "status": "completed",
   "definition_of_done": "scanSceneryCollisions accepts sceneryCount as a parameter instead of hard-coding the literal 3, making the collision bound explicit at the call site.",
   "notes": "The call site in main.cpp now passes constexpr size_t sceneryCount = 3, preserving the existing bound while removing the magic number from the helper.",
   "implementation": "In src/simulation.h, scanSceneryCollisions takes a size_t sceneryCount parameter. In src/main.cpp, the call passes constexpr size_t sceneryCount = 3.",
   "verification": "Verified by build and runtime: Release build succeeded, collision behavior unchanged (3 scenery entities still tested), no regression.",
   "commit": "ef1bbaa",
   "depends_on": [
    22,
    45
   ]
  },
  {
   "id": 47,
   "name": "EntityRole field for role-based movement/catch/chase",
   "status": "completed",
   "definition_of_done": "Entity carries an explicit EntityRole enum (Player/Scenery/Hostile), and all role-based logic in main.cpp and simulation.h uses the role field instead of raw index ranges.",
   "notes": "This eliminates the parallel identity mechanisms (index convention vs role) for movement, chase, and catch logic. buildInitialEntities assigns role alongside depth.",
   "implementation": "In src/entity.h, Entity gained EntityRole role defaulting to Scenery, and the EntityRole enum was added. In src/lifecycle.h, buildInitialEntities sets role for each entity. In src/simulation.h, chasePlayer uses role. In src/main.cpp, the catch test uses entity.role == EntityRole::Hostile.",
   "verification": "Verified by build and runtime: Release build succeeded, gameplay unchanged, player still caught by hostiles correctly.",
   "commit": "0eba3ae",
   "depends_on": [
    45
   ]
  },
  {
   "id": 48,
   "name": "Re-attempt stopEventSounds() for event audio bleed",
   "status": "completed",
   "definition_of_done": "Event audio (GAMEOVER.wav / win_sound.wav) is stopped when returning to MENU, preventing bleed into menu idle time.",
   "notes": "Root cause: ma_sound_stop() is the correct miniaudio API but the previous attempt failed due to timing/placement. This step re-attempted with corrected call placement.",
   "implementation": "In src/audio.h and src/main.cpp, the stopEventSounds() call was placed at the correct transition point.",
   "verification": "Verified by runtime: returning to MENU from GAME_OVER no longer lets event audio continue playing. The remaining cosmetic issue (short ~2.5s GAMEOVER clip post-Step 43) is acceptable.",
   "commit": "fc75b3d",
   "depends_on": [
    36,
    43
   ]
  },
  {
   "id": 49,
   "name": "Depth-based draw-order sorting in renderer.h",
   "status": "completed",
   "definition_of_done": "pe::Renderer::drawWorld sorts entities by Entity::depth before issuing draw calls (or applies an equivalent explicit ordering step), so draw order no longer relies implicitly on buildInitialEntities()'s construction order, while the current visual draw order (player, then scenery, then hostiles) remains unchanged for the existing entity set.",
   "notes": "Source evidence only, no design pre-decided at drafting time: src/renderer.h:349-353 already named this exact gap in its own comment inside drawWorld: 'Step 45: draw order relies on the entity vector being in depth order... If a future step reorders the vector (dynamic spawn, removal), sort by entity.depth here before iterating.' Entity::depth already existed (Step 45, commit e1bee39) and was already assigned correctly by buildInitialEntities() (0=player, 1=scenery, 2=hostile), but no sort ever read it - draw order was purely accidental construction order. Broad-phase collision was explicitly excluded from this step: rendering order and collision performance are separate concerns per the project's no-premature-abstraction discipline, and bundling them was rejected during scoping. Design questions resolved during implementation (left open at drafting time): the sort runs every frame inside drawWorld (negligible cost at stress-tested entity counts, avoids cache-invalidation complexity); std::stable_sort is used so entities sharing a depth keep their existing relative order; the sort call lives in renderer.h, not simulation.h, per explicit instruction.",
   "implementation": "src/renderer.h gained #include <algorithm> and drawWorld now builds a std::vector<size_t> permutation of entity indices, stable-sorted by entities[index].depth, computed fresh every frame before the draw loop. Neither entities nor colliding is reordered in place: the loop still looks up entities[i] and colliding[i] by the ORIGINAL index (the same i used by the still-index-based texture-select branches, i == 0 / i < 3), only visiting indices in depth-sorted order. On the current entity set this permutation is a no-op (construction order already matches depth order), so today's visual output is unchanged by construction, not by coincidence. No changes to entity.h, lifecycle.h, simulation.h, or main.cpp; drawAABBs (Step 42's debug overlay) was left untouched, out of this step's stated scope.",
   "verification": "Release build succeeded (cmake --build build --config Release, zero errors, both PureEngine.exe and hostile_data_test.exe produced). hostile_data_test.exe: PASS. Visual/overlap draw-order check: manually performed by Cyril (the assistant cannot see the rendered window). The player was driven into a hostile and into scenery; correct draw order/layering held under actual overlap, not just at rest. This satisfies the definition_of_done's requirement that draw order/layering be confirmed unchanged.",
   "depends_on": [
    45
   ]
  },
  {
   "id": 50,
   "name": "Unify renderer.h texture selection and AABB tint to Entity::role",
   "status": "completed",
   "definition_of_done": "renderer.h's texture-select and AABB debug-overlay tint logic both use Entity::role instead of raw index, closing the parallel identity mechanism gap.",
   "notes": "This is the last remaining raw-index spot in renderer.h. Zero behavior change: role and index agree for every entity since construction order is unchanged.",
   "implementation": "In src/renderer.h, drawWorld's texture-select block changed from i==0 / i<3 to entity.role==Player / entity.role==Scenery. drawAABBs' tint block changed from i==0 to entity.role==Player. Comment updated.",
   "verification": "Verified by build: Release build succeeded, no errors, no warnings.",
   "commit": "595ec9a",
   "depends_on": [
    47,
    49
   ]
  },
  {
   "id": 51,
   "name": "Role-based scenery collision scan (Phase 1)",
   "status": "completed",
   "definition_of_done": "scanSceneryCollisions refactored to collect player and scenery entities by role, eliminating hardcoded index ranges. All three playtest scenarios verified (collision tint+beep, hostile catch, player movement all four directions).",
   "notes": "Phase 1 simplifies collision logic by matching the role-gated patterns established in Step 47. Player lookup was already role-filtered (Step 47); this change role-filters the scenery collision pool. Behavior unchanged, code cleaner.",
   "implementation": "In src/simulation.h, scanSceneryCollisions now collects Player/Scenery-role entity indices into a pool and pair-tests those; the size_t sceneryCount parameter is removed. In src/main.cpp, the call site drops constexpr sceneryCount = 3 and passes only (entities, colliding); directly-touched comments updated. lifecycle.h/renderer.h comment cleanup deferred to future session.",
   "verification": "Verified by playtest: collision (red flash + beep on scenery hit), catch (GAME_OVER on hostile reach), movement (all four arrow directions work opposite-screen as expected under snap-follow camera).",
   "commit": "36977e6",
   "depends_on": [
    45,
    46,
    47
   ]
  },
  {
   "id": 52,
   "name": "Stress measurement: frame-time scaling at 50/500/2000/5000 hostiles",
   "status": "completed",
   "definition_of_done": "Measure average frame time at four hostile counts using stress-test fixtures. Determine if broad-phase spatial partitioning is needed based on scaling curve.",
   "notes": "Results show O(n) scaling: 1.21ms (50) -> 2.22ms (500) -> 5.46ms (2000) -> 12.09ms (5000). Engine handles 50-2000 hostiles comfortably. At 5000, approaching frame-budget limits but still playable. Broad-phase optimization deferred â€” recommended for future games needing 2000+ entities, not blocking Phase 2.",
   "implementation": "Used pre-built stress fixtures (hostile_stress_50/500/2000/5000.txt, tracked in commit 2d583d7). User ran each for ~15s survival, collected three 5-second frame-time averages per count. Frame reporter (main.cpp:813-877) printed 'avg frame: X ms' every 5s window.",
   "verification": "Verified by user playtest: four complete measurements collected, scaling curve plotted, verdict rendered.",
   "commit": "2d583d7",
   "depends_on": [
    51
   ]
  },
  {
   "id": 53,
   "name": "Depth layering fix: player foreground (depth=3)",
   "status": "completed",
   "definition_of_done": "Player entity renders on top of all other entities (scenery/hostile) by depth ordering. Confirmed visible on both scenery collision and hostile catch via playtest.",
   "notes": "One-line depth reassignment. Player now depth=3 (foreground), scenery=1 (mid-layer), hostile=2. Accepted side effect: player also covers hostiles during catch frame. Comment in entity.h (depth=0 is background) left stale, deferred to future cleanup sweep.",
   "implementation": "In src/lifecycle.h buildInitialEntities(), changed player.depth = 0 to player.depth = 3. No other changes.",
   "verification": "Verified by user playtest: player red-tint visible on scenery overlap, player red-flash visible on hostile catch, catch mechanic still ends run normally.",
   "commit": "1d4c68a",
   "depends_on": [
    51,
    52
   ]
  },
  {
   "id": 54,
   "name": "Genericize entity.h: roleId + parameter plumbing",
   "status": "completed",
   "definition_of_done": "Entity.role changed from arcade-specific enum to generic int roleId. Engine filters take explicit roleId parameters. All 25 role-comparison sites updated. Playtest verified: chase works, tint works, catch works.",
   "notes": "Introduces temporary parameter plumbing on simulation/renderer filters (caller-supplied role ids). Step 55 is expected to remove these params via index-slot optimization. ArcadeRole enum lives game-side in lifecycle.h; numeric mapping (0/1/2) preserved so behavior is identical.",
   "implementation": "entity.h: removed EntityRole enum, changed to int roleId=0. lifecycle.h: added ArcadeRole enum, assigned in buildInitialEntities. simulation.h/renderer.h: added roleId params. main.cpp: passed ArcadeRole values at call sites.",
   "verification": "Verified by playtest: hostile chase, scenery tint, catch mechanic. Player movement confirmed working (world-space correct, camera snap-follow visual confirmed).",
   "commit": "4c28d63",
   "depends_on": [
    47,
    51
   ]
  },
  {
   "id": 55,
   "name": "Genericize renderer.h: texture index slots",
   "status": "completed",
   "definition_of_done": "Renderer texture selection switched from role-gated branches to index-slot array. Role params deleted from drawWorld. All textureIds assigned uniquely: 0=player (green), 1=scenery (blue), 2=hostile (crimson), 3=hostile_alt (orange). Playtest verified all archetypes render correctly.",
   "notes": "Crux: textureId 0 was shared by three entity types â€” Step 55 reassigns slots to eliminate conflict. Data files rewritten for consistency (including all stress fixtures). drawAABBs playerRoleId param kept (debug highlighting, role-agnostic).",
   "implementation": "renderer.h: replaced named texture fields with entityTextures[4] array, deleted role params from drawWorld, added bounds-check fallback. lifecycle.h: assigned explicit textureIds (0/1). hostile_data.h: default textureId 0â†’2. All data files: updated with new slot numbers. main.cpp: dropped role args at drawWorld call.",
   "verification": "Verified by playtest: player green (0), scenery blue (1), hostile crimson (2), alt orange (3), collision tint, F1 overlay, both scenes.",
   "commit": "83f7477",
   "depends_on": [
    54
   ]
  },
  {
   "id": 56,
   "name": "Animation system foundation: data structures + Entity fields",
   "status": "completed",
   "definition_of_done": "animation.h defines Animation (frame sequence), AnimationFrame (index+duration), AnimationState (runtime playback state). Entity extended with animationState + animationSpeed fields. AnimationState::update(dt) advances frames autonomously. Non-breaking: Arcade/Pong both run unchanged. Verified: build clean, CTest 1/1, both games alive 6s.",
   "notes": "Step 56 is data structure only â€” no rendering integration yet (Step 57). AnimationState tracks current frame, elapsed time, playback state. Zero-duration frame hang fixed with bounded advance loop. Ownership contract documented (non-owning pointer, game-managed lifetime). Entity size +28 bytes (well under limit). No createAnimation helper (Part 3 rejected: header-only project convention, single-helper .cpp unjustified).",
   "implementation": "Created animation.h (AnimationFrame, Animation, AnimationState with bounded update + private advance helper). Extended entity.h: added #include, two new fields (animationState, animationSpeed), updated both ctors. No changes to existing functionality.",
   "verification": "Build clean (all 3 targets), CTest 1/1 passed (0.36s), Arcade alive 6s, Pong alive 6s, entity.h compiles animation.h successfully, +28 bytes per Entity (acceptable).",
   "commit": "6d3b1d2",
   "depends_on": []
  },
  {
   "id": 57,
   "name": "Renderer animation frame integration: UV calculation",
   "status": "completed",
   "definition_of_done": "Renderer calculates UV rects for spritesheet frames via calculateFrameUV(). drawWorld() accepts SpritesheetMetadata (framesPerRow, totalFrames). Per-entity frame resolution wired. Non-breaking: frame 0/full-texture default preserves existing visuals. Build clean, CTest 1/1, Arcade/Pong run unchanged.",
   "notes": "Header-only implementation (no separate renderer.cpp). V-axis orientation documented. Fixed VBO state leak (animated draws no longer corrupt static draws). totalFrames<=1 guard prevents sliver UV bug. Ready for Step 58 (data-driven animation files).",
   "implementation": "calculateFrameUV() in renderer.h computes UV rect from frameIndex+grid. Extended drawWorld() signature with SpritesheetMetadata. Updated draw loop to use frame UVs. Updated Arcade/Pong call sites (non-breaking defaults).",
   "verification": "Build clean (all 3 targets), CTest 1/1 (0.31s), Arcade alive 6s (visuals unchanged), Pong alive 6s (visuals unchanged).",
   "commit": "a00678e",
   "depends_on": [
    56
   ]
  },
  {
   "id": 58,
   "name": "Data-driven animation loading: animation_data.h + file parsing",
   "status": "completed",
   "definition_of_done": "animation_data.h implements pe::loadAnimations() with strict exception-free parsing, 3-candidate file probe, warn-and-skip on malformed lines. Loads animation definitions from assets/animation_default.txt. main.cpp integrated: animation tick loop added to PLAYING, first hostile assigned walk_left animation. CMake POST_BUILD copies asset. Parse proof: zero warnings on load. Build clean, CTest 1/1, Arcade/Pong run unchanged.",
   "notes": "Mirrors hostile_data.h discipline exactly (no-throw parsing, probe, fallback). Animation assignment placed before snapshot (reset-safe). Known limit: alt scene loses clips (will integrate in Step 59 lifecycle work). Tick loop uses dt*animationSpeed (animationSpeed field now has consumer).",
   "implementation": "Created animation_data.h: free function pe::loadAnimations(filename), 3-candidate probe, trim/parse helpers duplicated locally (coupling avoidance). Extended main.cpp: animation load at init, tick loop in PLAYING, role-lookup assignment for first hostile. Added CMake POST_BUILD copy for animation_default.txt.",
   "verification": "Build clean (all 3 targets, CMake reconfigured 22s), CTest 1/1 (0.63s), Arcade alive 7s (parse proof: zero warnings), Pong alive (unaffected), animation_default.txt loaded cleanly (walk_left + idle).",
   "commit": "ea6d80e",
   "depends_on": [
    57
   ]
  },
  {
   "id": 59,
   "name": "Animated Pong (per-entity spritesheet layout)",
   "status": "completed",
   "definition_of_done": "Entity cols/rows fields enable per-entity spritesheet layout. Pong paddles animated via paddle_animations.txt (move/idle clips). Visual proof: paddles cycle red-purple on W/S and UP/DOWN, idle on frame 0. Ball renders full (no sliver). Animation fluid, no regressions.",
   "notes": "Resolves architecture blocker: per-entity layout allows sheet + non-sheet textures in same drawWorld call. Ball stays 1x1 (full texture), paddles 8x1 (animated). Step 59A generated paddle_spritesheet.png procedurally.",
   "implementation": "Extended Entity: cols/rows fields (default 1x1). Updated drawWorld signature: removed global metadata, read per-entity. Pong: loaded paddle_animations.txt, assigned clips on key press, ticked animation in loop.",
   "verification": "Playtest: left paddle animates W/S, right paddle animates UP/DOWN, idle state, ball full texture, animation fluid.",
   "commit": "c83fb4d",
   "depends_on": [
    56,
    57,
    58
   ]
  },
  {
   "id": 60,
   "name": "Physics foundation: velocity and gravity fields",
   "status": "completed",
   "definition_of_done": "Entity gains velocity (Vec3) and gravityScale (float, default 0). physics.h defines GRAVITY constant and free functions (applyGravity, applyForce, integrate). Non-breaking: gravityScale=0 opt-in. Build clean, CTest 1/1, Arcade/Pong unchanged.",
   "notes": "Foundation only â€” no call sites yet. Step 61 wires gravity into simulation. Vec3 throughout (z=0), no Vec2. Massless model: force = acceleration. gravityScale=0 default preserves all existing entity behavior.",
   "implementation": "Created physics.h: GRAVITY constexpr(-9.8), applyGravity/applyForce/integrate free functions. Extended entity.h: velocity Vec3(0,0,0), gravityScale 0.0f, both ctors updated.",
   "verification": "Build clean (all 3 targets), CTest 1/1 (0.35s), Arcade alive 6s, Pong alive 6s, gravityScale=0 non-breaking confirmed.",
   "commit": "b9ca280",
   "depends_on": []
  },
  {
   "id": 61,
   "name": "Physics integration: applyPhysics in simulation.h",
   "status": "completed",
   "definition_of_done": "simulation.h gains applyPhysics() â€” gravity + velocity integration per entity. Gravity conditional, integration runs for any nonzero-velocity entity. Non-breaking: all existing entities have velocity=0, gravityScale=0.",
   "notes": "Semantic correction: gravity gated on gravityScale>0, integration runs for any nonzero velocity (velocity-without-gravity works correctly). No game-side changes.",
   "implementation": "Added applyPhysics() to simulation.h. Includes physics.h.",
   "verification": "Build clean, CTest 1/1, Arcade/Pong unchanged.",
   "commit": "073c792",
   "depends_on": [
    60
   ]
  },
  {
   "id": 62,
   "name": "Collision response: impulse-based resolveCollision",
   "status": "completed",
   "definition_of_done": "physics.h gains resolveCollision() â€” AABB overlap detection, push-out separation, velocity reflection with restitution [0,1]. Non-breaking: no callers yet.",
   "notes": "Lives in physics.h (not collision.h â€” detection stays pure). Equal-mass model. Static body limit documented (platformer prerequisite). Runtime proof pending first caller.",
   "implementation": "resolveCollision() in physics.h: overlap detection, push-out, impulse reflection, restitution clamp.",
   "verification": "Build clean, CTest 1/1, Arcade/Pong unchanged. Logic hand-verified (elastic case, guard cases).",
   "commit": "85bcb02",
   "depends_on": [
    60
   ]
  },
  {
   "id": 63,
   "name": "Tilemap system foundation",
   "status": "completed",
   "definition_of_done": "tilemap.h with Tile/Tilemap, loadTilemap (hostile_data discipline), tilemapToEntities, collideEntityWithTilemap (returns axis normal). Sample assets/tilemap_default.txt. 3 proof cases in existing test binary. Non-breaking.",
   "notes": "Render path = synthesize entities -> existing drawWorld (second call). tileId==0 = empty. World mapping documented. Two deviations accepted: loadTilemap takes const std::string& (sibling style); empty-range loop fixed pre-commit.",
   "implementation": "src/tilemap.h (~290 lines), assets/tilemap_default.txt (10x8), +140 lines in hostile_data_test.cpp",
   "verification": "Build clean, CTest 1/1 (8 cases total), Arcade/Pong unchanged (do not include header).",
   "commit": "786381d",
   "depends_on": [
    62
   ]
  },
  {
   "id": 64,
   "name": "Scene management foundation",
   "status": "completed",
   "definition_of_done": "scene.h with Scene (name/entities/tilemap) and SceneManager. loadScene (find-or-create), switchTo, clearCurrent, addEntity, removeEntity, loadTilemapIntoScene. Safe no-ops on all failure paths. Proof cases in existing test binary. Non-breaking.",
   "notes": "loadTilemapIntoScene returns true only when map is stored; false leaves previous tilemap untouched. loadScene does not change current. Pointer invalidation documented. No auto tilemap->entity conversion.",
   "implementation": "src/scene.h (~150 lines), +128 lines in hostile_data_test.cpp",
   "verification": "Build clean, CTest 1/1 (10 cases), Arcade/Pong untouched and alive.",
   "commit": "4b7f06d",
   "depends_on": [
    63
   ]
  },
  {
   "id": 65,
   "name": "Transform hierarchy foundation",
   "status": "completed",
   "definition_of_done": "Entity gains parentIndex (-1 = root). setParent with cycle detection. worldPosition walks the chain (translation-only). Bounded walk terminates even on hand-written cycles. Proof cases in existing test binary. Non-breaking.",
   "notes": "Only entity.h touched. No consumer rewiring. setParent refuses detached loops. parentless entities remain identical (worldPosition == position).",
   "implementation": "src/entity.h (+99), +112 lines in hostile_data_test.cpp",
   "verification": "Build clean, CTest 1/1 (13 cases), Arcade/Pong untouched and alive.",
   "commit": "3adc2ea",
   "depends_on": [
    64
   ]
  },
  {
   "id": 66,
   "name": "Font/Text rendering foundation",
   "status": "completed",
   "definition_of_done": "font.h with fontCellFor/textWidth/alignOffsetX. drawTextString added to renderer (drawDigitString untouched). Atlas extended with A-Z while cells 0-10 stay byte-identical. Proof cases in existing test binary. Non-breaking.",
   "notes": "Screen-space only. Lowercase folds to upper. Existing digit path fully preserved. Atlas proof via pixel decode-compare.",
   "implementation": "src/font.h (~70), renderer.h (+64), make_font.ps1 updated, font_digits.png extended, +57 test lines",
   "verification": "Build clean, CTest 1/1 (15 cases), Arcade/Pong untouched and visually identical.",
   "commit": "09bc6a0",
   "depends_on": [
    65
   ]
  },
  {
   "id": 67,
   "name": "Event system foundation",
   "status": "completed",
   "definition_of_done": "events.h with EventType, GameEvent, EventBus (subscribe/unsubscribe/emit/clear/handlerCount). Snapshot emit for reentrancy safety. Token non-reuse. Proof cases in existing test binary. Non-breaking.",
   "notes": "Single-threaded only. Opaque int payloads. No game wiring yet. Extension rule: new types append above Count.",
   "implementation": "src/events.h (~150 lines), +149 lines in hostile_data_test.cpp",
   "verification": "Build clean, CTest 1/1 (18 cases), Arcade/Pong untouched.",
   "commit": "c12ceb7",
   "depends_on": [
    66
   ]
  },
  {
   "id": 68,
   "name": "Debug console foundation",
   "status": "completed",
   "definition_of_done": "console.h with Console, toggle/feedKey/submit/print/registerCommand/drawConsole. Built-ins: help, clear, echo. History capped at 64. Proof cases in existing test binary. Non-breaking.",
   "notes": "No backdrop panel. Commands case-insensitive. Extra safety: rejects null handlers and builtin-name collisions. GLFW_INCLUDE_NONE used to avoid glad conflict. drawConsole not executed headless (same pattern as prior steps).",
   "implementation": "src/console.h (~290 lines), +159 test lines, small CMakeLists.txt header path addition for test target",
   "verification": "Build clean, CTest 1/1 (22 cases), Arcade/Pong untouched.",
   "commit": "ff97e4c",
   "depends_on": [
    67
   ]
  },
  {
   "id": 69,
   "name": "Gamepad input foundation",
   "status": "completed",
   "definition_of_done": "gamepad.h with GamepadState, pollGamepad, applyDeadzone, gamepadButton, gamepadButtonEdge. Raw GLFW codes. Deadzone support. Proof cases in existing test binary. Non-breaking.",
   "notes": "input.h completely untouched. No force feedback. Hardware connected path code-reviewed but not executed in this environment. Test target now links glfw for the poll symbols.",
   "implementation": "src/gamepad.h (~130 lines), +113 test lines, small CMakeLists.txt link addition for test target",
   "verification": "Build clean, CTest 1/1 (26 cases), Arcade/Pong untouched.",
   "commit": "2c97064",
   "depends_on": [
    68
   ]
  },
  {
   "id": 70,
   "name": "Particle system foundation",
   "status": "completed",
   "definition_of_done": "particles.h with Particle, Emitter, spawnParticle/emit/updateParticles/particlesToEntities. Swap-remove pool, capped emitter, drawWorld converter. Proof cases in existing test binary. Non-breaking.",
   "notes": "Renders as textured triangles via drawWorld (no quads/points yet). Color stored, not rendered. No game wiring yet. First std::rand use in src/.",
   "implementation": "src/particles.h (~170 lines), +179 lines in hostile_data_test.cpp",
   "verification": "Build clean, CTest 1/1 (31 cases), Arcade/Pong untouched.",
   "commit": "d7245fa",
   "depends_on": [
    69
   ]
  },
  {
   "id": 71,
   "name": "Arcade integration sprint",
   "status": "completed",
   "definition_of_done": "Arcade adopts tilemap (arena asset, second drawWorld, player collision), SceneManager ownership + switching, EventBus collision/scene-change paths, console (toggle/type/submit/draw, PLAYING+PAUSED), gamepad (stick+A/START), catch-burst particles, state labels + console via drawTextString, hierarchy satellite + renderer world-position branch. Non-breaking: catch timing identical (0.55s).",
   "notes": "VM ghost key events observed during playtests (phantom SPACE/ESC/2 edges incl. untouched paths; gamepad proven innocent via probe: mapped=0 always). Not a game bug. C2360 lesson: no case-level declarations. Duplicate GLFW_KEY_2 in tracked list is benign.",
   "implementation": "src/main.cpp (+~300), lifecycle.h (satellite), renderer.h (parent branch), arcade_arena.txt, CMake asset copy",
   "verification": "Build clean, CTest 1/1, scripted playtests (scene events, deaths, console help/entities over stdout), Pong alive.",
   "commit": "e1488f2",
   "depends_on": [
    70
   ]
  },
  {
   "id": 72,
   "name": "Physics hardening for platformer",
   "status": "completed",
   "definition_of_done": "Entity gains isStatic + controller fields. resolveCollision honors statics (full correction on dynamic, both-static no-op). updateCharacterController: gravity/clamp/integrate, single min-axis static resolve, jump + coyote + ceiling cancel, grounded state. 8 behavior tests green (39 total).",
   "notes": "Two real findings while testing: split H-then-V pass shoved landing characters sideways (fixed: single min-axis pass); grounded tolerance trips one frame before rest (tests settle, engine correct). No tunneling protection; no entity-entity static platform path beyond resolve.",
   "implementation": "src/entity.h (+fields), src/physics.h (+static resolve, controller), +~200 test lines",
   "verification": "Build clean, CTest 1/1 (39 cases). Fall probe used during diagnosis (temp, outside repo).",
   "commit": "9ac9246",
   "depends_on": [
    71
   ]
  },
  {
   "id": 73,
   "name": "Camera lerp follow (followLerp)",
   "status": "completed",
   "definition_of_done": "Camera::followLerp() added to camera.h. Arcade uses lerp (speed=5.0) instead of snap. Platformer/Pong keep snap. Non-breaking: sim unchanged (0.55s canary identical).",
   "notes": "Purely additive â€” follow() untouched. Blend clamped [0,1] against low-fps overshoot. Arcade movement now feels smoother without touching physics.",
   "implementation": "Added followLerp(target, dt, speed=5.0) to camera.h. One call-site swap in main.cpp:1174.",
   "verification": "Build clean, CTest 45/45, arcade 0.55s death identical, Pong alive.",
   "commit": "db53a13",
   "depends_on": []
  },
  {
   "id": 74,
   "name": "Platformer proof game (v1.0 gate)",
   "status": "completed",
   "definition_of_done": "Minimum platformer game proves engine systems work together: tilemap, scenes, physics controller, animation, EventBus, audio, particles, text, console, gamepad path. Full TITLE->L1->L2->WIN flow verified by human playtest.",
   "notes": "Movement, jump, gravity, landing, walls, L1 goal, L2 goal, WIN, text, console all instrument-verified. Particles + audio human-verified. VK_UP environmental (VM only, W/Space cover jump). Arcade and Pong unchanged. Input snapshot must postdate sim reads (spc=1/jmp=0 lesson).",
   "implementation": "games/platformer/platformer.cpp. Two tilemap levels (assets/platformer_level1.txt, assets/platformer_level2.txt). Input snapshot ordering fix (moved post-sim). A/D movement mapping. sceneByName finder.",
   "verification": "Human playtest: 15/15 items pass. CTest 46/46. Arcade/Pong unchanged and alive.",
   "commit": "4187cfe",
   "depends_on": [
    60,
    61,
    62,
    63,
    64
   ]
  },
  {
   "id": 75,
   "name": "Audio volume control (master + sfx gain)",
   "status": "completed",
   "definition_of_done": "Audio gains masterVolume + sfxVolume fields. setMasterVolume/setSfxVolume API. applyVolumes() applies to all initialized sounds. Console command 'volume' in arcade. Headless clamp test. Non-breaking at defaults (x1.0 = identity).",
   "notes": "All current sounds are SFX (no music stream). Per-sound volume deferred to v1.2. sfxVolume exists as designated v1.2 split point. Uniform gain: every sound plays at master x sfx. clampVolume01 kept private per spec; tested through public setters (stronger). Live console echo unachieved (delivery dead spell); compensated by VOL-PROBE-PASS on real initialized sounds.",
   "implementation": "audio.h: 4 public methods + private applyVolumes + clampVolume01. main.cpp: volume console command. tests: clamp assertions via public API.",
   "verification": "Build clean, CTest 47/47, VOL-PROBE-PASS (init/set/play/clamp/shutdown exit 0), console command code-reviewed (live echo blocked by environment), 0.550s canary unchanged.",
   "commit": "15a7f3e",
   "depends_on": []
  },
  {
   "id": 76,
   "name": "64x64 procedural player sprite",
   "status": "completed",
   "definition_of_done": "tex_player.png upgraded from 16x16 checkerboard to 64x64 ship sprite. make_textures.ps1 updated with width/height params. Engine renderer unchanged (resources.h handles any resolution). Scenery/hostile textures byte-identical.",
   "notes": "Renderer supports any resolution by construction (stbi_load + normalized UVs). Zero engine changes needed. Existing scenery/hostile regenerate byte-identical. Zero zero-red pixels (tint rule safe). 104 distinct colors. Human eye verification of ship appearance pending (headless here).",
   "implementation": "make_textures.ps1: added width/height params. assets/tex_player.png: regenerated as 64x64 ship.",
   "verification": "Build clean, CTest 47/47, arcade 0.550s canary unchanged, 64x64 decode verified, on-screen size identical by construction (same UV rect + entity scale).",
   "commit": "e8cd676",
   "depends_on": []
  },
  {
   "id": 77,
   "name": "Shader system: loadable GLSL files",
   "status": "completed",
   "definition_of_done": "src/shader.h implements loadShader(vert,frag). GLSL moved from hardcoded strings to assets/shaders/default.vert + .frag. Renderer::init() loads from files. Visual output identical. Non-breaking.",
   "notes": "Enables custom shaders per game. Future: 2D lighting shader (Step 79). Default shader is exact extract of previous hardcoded GLSL (proven byte-identical, not by eye). Base filenames + internal probe (tilemap precedent), not full paths.",
   "implementation": "shader.h: readShaderFile/compileShader/loadShader free functions. assets/shaders/: two GLSL files. renderer.h: init() calls loadShader(). CMakeLists: POST_BUILD shader copy x3. package.ps1 stages shaders dir.",
   "verification": "Build clean, CTest 47/47, canary 0.550s unchanged, all 3 games alive on file-loaded shaders.",
   "commit": "0819b1e",
   "depends_on": []
  },
  {
   "id": 78,
   "name": "Sprite batching (texture-group draw call reduction)",
   "status": "completed",
   "definition_of_done": "drawWorld() batches entities by textureId. O(n) draw calls -> O(texture groups). Measured frame time improvement at 2000/5000 entities. Visual output identical. Non-breaking.",
   "notes": "Texture groups: max 5 slots (player/scenery/hostile/alt/paddle). Step 52 baseline: 5.5ms@2000, 12ms@5000. Post-batch: 4.9ms@2000, 10.4ms@5000.",
   "implementation": "renderer.h drawWorld(): sort by depth, group by textureId, batch-upload per group, one glDrawArrays per entity within group (uniforms per entity). VBO uploads reduced from O(n) to O(texture groups); texture binds from O(n) to O(texture groups).",
   "verification": "Build clean (4/4), CTest 47/47, canary 0.550s unchanged, stress test: 4.9ms@2000 (12% improvement), 10.4ms@5000 (14% improvement). Pong/Platformer alive.",
   "commit": "b1a1acb",
   "depends_on": [
    77
   ]
  },
  {
   "id": 79,
   "name": "2D point lighting (lit.frag + LightingState)",
   "status": "completed",
   "definition_of_done": "lighting.h defines PointLight + LightingState. lit.frag adds point light attenuation. drawWorld() overload accepts LightingState. Default behavior unchanged. Arcade demonstrates player light source.",
   "notes": "Up to 4 point lights per frame. No shadows (v2.0). Default shader unchanged. Non-breaking: existing calls use flat ambient. Deviation: CMakeLists unchanged — shaders already copied via copy_directory (no per-file POST_BUILD needed).",
   "implementation": "lighting.h: structs. lit.vert/lit.frag: attenuation 1/(1+(d*d)/(r*r)). renderer.h: litProgram + litTransform/litColor + setLightUniforms (clip-space transform) + drawWorldInternal helper (no duplication) + overloads. main.cpp: PLAYING uses warm player light (ambient 0.15,0.15,0.2 + light 1,0.8,0.6 r6), other states flat.",
   "verification": "Build clean (4/4), CTest 47/47, canary 0.549s unchanged (PLAYING lighting visual only), Pong/Platformer alive, lit shaders copied to build/Release/assets/shaders, visual lighting verified (ambient dark + warm light follows player, edges darker).",
   "commit": "e9fc4de",
   "depends_on": [
    77,
    78
   ]
  },
  {
   "id": 80,
   "name": "Per-sound volume (Beep/GameOver/NewHighScore)",
   "status": "completed",
   "definition_of_done": "audio.h enum Sound + get/setVolume per sound (master*sfx*perSound), defaults 1.0 clamp [0,1], safe pre-init. Arcade sets Beep 0.5 after init (collision quieter, events full). Header-only, non-breaking.",
   "notes": "Builds on Step75 master*sfx. No new deps, no batching/lighting/scene/physics changes.",
   "implementation": "audio.h: enum Sound, beep/gameOver/newScore vol floats, get/setVolume, applyVolumes base*perSound. main.cpp: audio.setVolume(Beep,0.5f) proof.",
   "verification": "Build clean (4/4), CTest 47/47, arcade/platformer alive, per-sound volumes applied.",
   "commit": "1478208",
   "depends_on": [
    75
   ]
  },
  {
   "id": 81,
   "name": "Scene serialization (scene_*.txt v1)",
   "status": "completed",
   "definition_of_done": "scene.h extends Scene with tilemapFile and save/loadSceneFromFile (text v1, strict whole-file fallback, atomic tmp+rename). Round-trip save->load asserts entity count/positions/tilemap match. Header-only, non-breaking.",
   "notes": "Reuses 3-candidate probe + loadTilemap discipline. No binary/versioning beyond # scene v1 header. Tilemap reference basename only.",
   "implementation": "scene.h: Scene.tilemapFile, sceneTrim/parse helpers, saveSceneToFile (fixed 4), loadSceneFromFile (strict), saveSceneManagerToFile optional. loadTilemapIntoScene sets tilemapFile. Test checkSceneSerialization round-trip + strict fallback.",
   "verification": "Build clean (4/4), CTest 48/48 (new test), arcade/platformer alive, round-trip proved.",
   "commit": "45cc107",
   "depends_on": [
    64
   ]
  },
  {
   "id": 82,
   "name": "Menu instructions + HUD labels (TIME/BEST)",
   "status": "completed",
   "definition_of_done": "MENU draws 3 centered instruction lines via drawTextString; PLAYING HUD shows TIME/BEST labels additive next to digit rows via ui.h. No new fonts/headers, existing positions kept. Display proven.",
   "notes": "Additive only: menu readable, HUD labels visible right of digits (+3.0), no overlap, no new layout system.",
   "implementation": "ui.h: drawHud adds TIME/BEST drawTextString before digits. main.cpp: else-if MENU draws 3 centered lines (SPACE START 2 ALT / ARROWS/WASD MOVE ESC PAUSE / AVOID RED SURVIVE).",
   "verification": "Build clean (4/4), CTest 48/48, arcade menu alive 3s with new text, HUD labels visible additive.",
   "commit": "b114f96",
   "depends_on": [
    66
   ]
  },
  {
   "id": 83,
   "name": "Orphan annotation + contract freeze (docs-only)",
   "status": "completed",
   "definition_of_done": "simulation.h applyPhysics and particles.h Emitter::emit marked foundation-only; camera.h header states arcade uses follow()/followLerp() and move() retained; entity.h hierarchy frozen as translation-only with re-establish rule. No behavior change, docs only.",
   "notes": "No wiring of orphans, no TRS, no follow change. Header comments only.",
   "implementation": "simulation.h: applyPhysics comment foundation-only; particles.h: emit comment foundation-only; camera.h: contract freeze follow/move; entity.h: hierarchy attachment-only + invalid after erase note.",
   "verification": "Build clean (4/4), CTest 48/48, no behavior change.",
   "commit": "de4baa4",
   "depends_on": [
    61,
    65,
    70
   ]
  },
  {
   "id": 84,
   "name": "OOB textureId log + doc refresh (83->84, shader/lighting taxonomy)",
   "status": "completed",
   "definition_of_done": "renderer.h logs OOB textureId [0..4] to cerr then falls back to checker; README/AGENTS step count 83 and header taxonomy updated (shader.h/lighting.h added, 48 cases). No behavior change for valid ids.",
   "notes": "One-line cerr per OOB group (debug spam acceptable). Tilemap path unchanged (uses same fallback).",
   "implementation": "renderer.h: BatchGroup OOB check with cerr before checker fallback. README.md: 74->83 steps, add shader.h/lighting.h layout, 46->48 cases. AGENTS.md: add shader/lighting boundaries.",
   "verification": "Build clean (4/4), CTest 48/48, no runtime change for valid ids.",
   "commit": "eecbb9d",
   "depends_on": [
    55
   ]
  },
  {
   "id": 85,
   "name": "Pong score + win (first to 5)",
   "status": "completed",
   "definition_of_done": "Pong tracks left/right score, ball past paddle awards point and resets ball, first to 5 wins with freeze + label, scores drawn via digit/text path. Minimal complete rules only.",
   "notes": "No new headers, no audio/lighting/batching/physics beyond Pong. Arcade/platformer untouched.",
   "implementation": "games/pong/pong.cpp: left/rightScore, winScore=5, win flag/winner, scoring on x<-7/x>7 with ball reset and velocity flip, win freeze, HUD digits at -2.0/1.5 and win text centered. Test checkPongScore pure logic.",
   "verification": "Build clean (4/4), CTest 49/49 (new pong score test), Pong alive with scores/win, arcade/platformer untouched.",
   "commit": "5c70453",
   "depends_on": []
  },
  {
   "id": 86,
   "name": "Particle system depth (color via tint + emit wired)",
   "status": "completed",
   "definition_of_done": "Particle.color renders via Entity.tint; particlesToEntities passes p.color; Emitter::emit wired via unit test (and burst path unchanged). Header-only, non-breaking, no new features.",
   "notes": "Minimal depth — makes existing system honest. Burst remains white, emitter colors now visible.",
   "implementation": "entity.h: Vec3 tint (1,1,1) + ctors; particles.h: tint passthrough + header note update; renderer.h: colliding ? red : entity.tint; test checkParticleColorAndEmit verifies tint + emit no longer orphan.",
   "verification": "Build clean (4/4), CTest 49/49 (new test), arcade burst + emitter path working.",
   "commit": "ec4ccf2",
   "depends_on": [
    70
   ]
  },
  {
   "id": 87,
   "name": "Optional fixed-timestep (prevent 1-unit tunnel)",
   "status": "completed",
   "definition_of_done": "physics.h adds updateCharacterControllerFixed/applyPhysicsFixed (split large dt into 1/60 substeps, clamped). Default remains variable-dt; opt-in via Fixed wrappers. Test proves large dt no longer tunnels 1-unit tile. Header-only, non-breaking.",
   "notes": "No broadphase, no resolve math change, no new systems. Substeps capped at 8 to avoid spiral.",
   "implementation": "physics.h: two Fixed helpers (ceil dt/fixedDt, loop substeps, jump consumed once). Test checkFixedTimestepNoTunnel: floor+player with -20 vel dt 0.1 -> Fixed stays grounded, no tunnel.",
   "verification": "Build clean (4/4), CTest 50/50 (new test), arcade/platformer alive.",
   "commit": "86e892a",
   "depends_on": [
    72
   ]
  },
  {
   "id": 88,
   "name": "Basic input action map (Move/Jump/Pause/Confirm)",
   "status": "completed",
   "definition_of_done": "input.h adds Action enum + keysForAction + isActionDown/isActionEdge (thin mapping over raw GLFW codes). Raw isDown/isEdge unchanged. Platformer jump demo uses isActionEdge(Jump). Header-only, non-breaking.",
   "notes": "No rebind UI, no contexts, no buffering. Mapping: Move 4 dirs, Jump (SPACE/W/UP), Pause (ESC), Confirm (SPACE/ENTER), Back (ESC/BACKSPACE).",
   "implementation": "input.h: Action enum, keysForAction switch, Input::isActionDown/Edge loops over mapped keys. platformer.cpp: jumpEdge via isActionEdge(Jump). Test checkActionMap verifies mappings.",
   "verification": "Build clean (4/4), CTest 51/51 (new action map test), platformer jump via Action, arcade raw path untouched.",
   "commit": "7b339c6",
   "depends_on": [
    16
   ]
  },
  {
   "id": 89,
   "name": "Texture slot growth + registry (growable vector, 6th texture)",
   "status": "completed",
   "definition_of_done": "renderer.h replaces fixed [5] with vector registry, keep 0..4 identical, thin registerTexture helper, OOB checker fallback, 6th texture proof (id 5) via reuse of tex_player.png. No asset manager, no hot-reload.",
   "notes": "Growable only — no unloading/hot-reload. Checker fallback preserved for OOB.",
   "implementation": "renderer.h: vector<GLuint> entityTextures, reserve(8), push 0..4, registerTexture() returns new id, OOB check via size(), destroyAll loops vector, proof in init loads id5 tex_player.png.",
   "verification": "Build clean (4/4), CTest 52/52 (new registry test), 6th texture id 5 valid, OOB fallback to checker.",
   "commit": "94e7189",
   "depends_on": [
    55
   ]
  },
  {
   "id": 90,
   "name": "Console history + scrollback (Up/Down recall, 64 ring)",
   "status": "completed",
   "definition_of_done": "Console stores last 64 submitted lines; Up/Down recall while open via feedKey; existing feedKey/submit/print/draw unchanged; test proves store/recall/cap. Header-only, non-breaking.",
   "notes": "Minimal ring, no panel/mouse/search. Input tracks UP/DOWN for edge, pumped in arcade/platformer.",
   "implementation": "console.h: submitHistory + historyPos, submit pushes ring, feedKey UP/DOWN recall. main.cpp/platformer.cpp: Input tracks UP/DOWN, pumps to console. Test checkConsoleHistoryRecall verifies recall and cap.",
   "verification": "Build clean (4/4), CTest 53/53 (new history test), console recall proven.",
   "commit": "f32760a",
   "depends_on": [
    68
   ]
  },
  {
   "id": 91,
   "name": "Event system polish (noexcept, once, throw-safe)",
   "status": "completed",
   "definition_of_done": "events.h adds noexcept doc, once() auto-unsub, emit() try/catch; test proves throwing handler not breaking bus and once fires once.",
   "notes": "Header-only, non-breaking, minimal. Batch-verified with 92-99 via 596f944 — no per-step isolated build/test.",
   "implementation": "events.h:29-31 noexcept doc + 92-101 emit() try/catch snapshot + 111-124 once() auto-unsub; tests/hostile_data_test.cpp:1970 checkEventThrowAndOnce (throwing handler skipped, once fires once, clear() 0). Batch commit 596f944 (9 steps, not per-step isolated).",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed (hostile_data_test incl. checkEventThrowAndOnce); batch-verified via 596f944 with 92-99, no per-step isolated build.",
   "commit": "596f944",
   "depends_on": [
    67
   ]
  },
  {
   "id": 92,
   "name": "Time scale / pause-aware clock",
   "status": "completed",
   "definition_of_done": "FrameTime adds timeScale (default 1.0) and paused flag with setPaused/setTimeScale and scaledTick() (0 when paused else tick*scale); wall time via tick(). Opt-in.",
   "notes": "Header-only, non-breaking, minimal. Batch-verified with 91,93-99 via 596f944.",
   "implementation": "time.h:87-97 FrameTime timeScale/paused + setTimeScale/getTimeScale/setPaused/isPaused/scaledTick(); time.h:103-104 members timeScale=1.0 paused=false. tests/hostile_data_test.cpp:2055 checkTimeScale (default 1.0/ not paused, clamp, scaledTick 0 when paused). Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (checkTimeScale); batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    17
   ]
  },
  {
   "id": 93,
   "name": "Animation clip switch helper",
   "status": "completed",
   "definition_of_done": "animation_data.h adds setClip(Entity&,map,name) safe lookup, assigns without dangling, preserves elapsed on same clip.",
   "notes": "Header-only, non-breaking, minimal. Batch-verified with 91-92,94-99 via 596f944.",
   "implementation": "animation_data.h:14 #include entity.h + 182-193 setClip(Entity&,map,name) safe lookup, no dangling, preserves elapsed on same clip. tests/hostile_data_test.cpp:2069 checkAnimationClipSwitch (switch, missing, same-clip preserves). Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (checkAnimationClipSwitch); batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    56
   ]
  },
  {
   "id": 94,
   "name": "Basic resource unload",
   "status": "completed",
   "definition_of_done": "renderer.h adds releaseTexture(id) and clearNonCoreTextures() (keep 0..4), thin helper, no hot-reload.",
   "notes": "Header-only, non-breaking, minimal. Thin: no dedicated test, batch-verified with 91-93,95-99 via 596f944.",
   "implementation": "renderer.h:794 releaseTexture(int) + 801 clearNonCoreTextures() keep 0..4 alive, vector registry (Step 89). No dedicated test; exercised via build + alive probes. Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (no dedicated unload test — thin verification, batch); Platformer/Arcade alive. Batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    89
   ]
  },
  {
   "id": 95,
   "name": "Console multi-line / wrap (minimal)",
   "status": "completed",
   "definition_of_done": "console.h pushLine wraps >38 cols into multiple lines for existing draw area; print() benefits. No panel.",
   "notes": "Header-only, non-breaking, minimal. Thin: wrap has no dedicated test, batch-verified with 91-94,96-99 via 596f944.",
   "implementation": "console.h:79-88 pushLine() wrap at 38 cols, splits long lines, respects MAX_HISTORY 64. Implicit via console tests (checkConsoleHistory etc). Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (console tests 22 cases, no dedicated wrap test — thin); batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    68
   ]
  },
  {
   "id": 96,
   "name": "Debug overlay expansion",
   "status": "completed",
   "definition_of_done": "main.cpp F1 overlay adds ENTS/TILES/DT stats via drawTextString, cheap, optional.",
   "notes": "Header-only, non-breaking, minimal. Visual-only verification, batch-verified with 91-95,97-99 via 596f944.",
   "implementation": "main.cpp:1642-1651 F1 debug AABBs + stats ENTS/TILES/DT via drawTextString(-5.75,2.4). Relies on renderer.h drawAABBs:639 and drawTextString. Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed; manual F1 probe (visual) — not machine-tested, batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    42
   ]
  },
  {
   "id": 97,
   "name": "Simple spatial helper (grid/hash)",
   "status": "completed",
   "definition_of_done": "collision.h adds broadphaseGrid(entities,cellSize) bucket by position, returns pairs sharing cell. Does not replace O(n).",
   "notes": "Header-only, non-breaking, minimal. Foundation-only, no caller yet (optional), batch-verified with 91-96,98-99 via 596f944.",
   "implementation": "collision.h:51-53 includes <unordered_map>,<utility>,<vector> + 93-116 broadphaseGrid(entities,cellSize) buckets by floor(pos/cellSize), returns pairs sharing cell, no O(n) replacement. No dedicated test (optional foundation). Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (no dedicated broadphase test — foundation-only); batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    8
   ]
  },
  {
   "id": 98,
   "name": "Audio music / loop slot",
   "status": "completed",
   "definition_of_done": "audio.h adds musicSound, musicLoaded, musicVolume, playMusicLoop/stopMusic/setMusicVolume; master*musicVol distinct from SFX, loopable.",
   "notes": "Header-only, non-breaking, minimal. Thin: no dedicated test, batch-verified with 91-97,99 via 596f944.",
   "implementation": "audio.h:255-264 playMusicLoop(file,loop) + getMusicVolume/setMusicVolume, members musicSound/musicLoaded/musicVolume, 322-325 applyVolumes() master*musicVol, 268 stop in shutdown. Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (no dedicated music test — thin); audio alive probes. Batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    20
   ]
  },
  {
   "id": 99,
   "name": "Hierarchy contract test + final freeze",
   "status": "completed",
   "definition_of_done": "tests adds checkHierarchyContractFreeze proving attachment-only (translation) and erase invalidates; headers freeze TRS out of scope.",
   "notes": "Header-only, non-breaking, minimal. Batch-verified with 91-98 via 596f944; TRS out of scope for v1.",
   "implementation": "entity.h:93-101 hierarchy freeze comment attachment-only + TRS deferred; tests/hostile_data_test.cpp:2258 checkHierarchyContractFreeze (translation-only, erase invalidates). Batch 596f944.",
   "verification": "cmake --build build --config Release -> zero errors; ctest 1/1 Passed (checkHierarchyContractFreeze); batch-verified via 596f944.",
   "commit": "596f944",
   "depends_on": [
    65
   ]
  },
  {
   "id": 100,
   "name": "Engine freeze + v1.0 readiness notes (docs-only)",
   "status": "completed",
   "definition_of_done": "README/AGENTS/PURE_ENGINE_V3.md updated to 100 steps, explicit In/Frozen/Deferred lists, no new code. Engine frozen.",
   "notes": "No ECS, no broadphase, no TRS, no editor/net. Future layers deferred.",
   "implementation": "Docs only: README step count 100, PURE_ENGINE_V3.md Steps 25-100 + roadmap 91-100 + freeze section.",
   "verification": "Build clean, CTest 54/54, docs reflect 100 steps.",
   "commit": "dadd76b",
   "depends_on": []
  },
  {
   "id": 101,
   "name": "Resource cache & binary blob (loadBinaryBlob + pack entry)",
   "status": "completed",
   "definition_of_done": "resources.h adds loadBinaryBlob (3-candidate probe, vector<uint8_t>) and loadPackEntry (pack fallback) plus binaryCache/clear, keeps text/PNG/WAV probe unchanged, header-only.",
   "notes": "Pack entry currently pack file or direct fallback; cache optional.",
   "implementation": "resources.h: loadBinaryBlob, loadPackEntry, binaryCache, loadBinaryBlobCached, clearBinaryCache. Test checkBinaryBlob proves beep.wav blob + cache + pack fallback.",
   "verification": "Build clean (4/4), CTest 55/55 (new blob test), existing assets unchanged.",
   "commit": "17975c9",
   "depends_on": [
    14
   ]
  },
  {
   "id": 102,
   "name": "pe_core static lib (header-only still alive)",
   "status": "completed",
   "definition_of_done": "CMake adds pe_core STATIC lib (src/pe_core.cpp + stb_impl + glad), header-only path stays alive (games still include headers directly), arcade/platformer/Pong build in both modes, no behaviour change.",
   "notes": "Optional compiled core, games keep header-only; pe_core links glfw/miniaudio, provides pe_core_version().",
   "implementation": "src/pe_core.cpp includes all core headers, add_library(pe_core STATIC ...), target_include/link. Header-only still works.",
   "verification": "Build clean (4/4 + pe_core.lib), CTest 55/55, arcade/platformer/Pong alive both modes.",
   "commit": "7bce2c2",
   "depends_on": []
  },
  {
   "id": 103,
   "name": "Lightweight component helpers (health/tag/timer/velocity)",
   "status": "completed",
   "definition_of_done": "entity.h adds health/timer/tag plain fields (defaults 100/0/empty); components.h adds free-function helpers; entities remain plain structs, helpers opt-in, games unchanged.",
   "notes": "No archetype, no mandatory components, no query language.",
   "implementation": "entity.h: health/timer/tag + ctors; components.h: inline helpers; test checkComponentHelpers proves health/tag/timer/velocity.",
   "verification": "Build clean (4/4), CTest 56/56 (new component test), games alive.",
   "commit": "364d52b",
   "depends_on": []
  },
  {
   "id": 104,
   "name": "Editor-time scene dump/load (console commands)",
   "status": "completed",
   "definition_of_done": "Console commands scene_dump and scene_reload. Round-trip test proves entity count + position + tilemap survive serialization. Uses Step 81 format exactly.",
   "notes": "No visual editor. No new format. Reuses saveSceneToFile/loadSceneFromFile. savedata/ already exists.",
   "implementation": "main.cpp: two console commands (dump to savedata/scene_dump.txt with counts, reload replaces activeScene entities/tilemap and rebuilds cache). scene.h: save/load handle explicit savedata/ paths via filesystem create_directories and 4-candidate probe. Test checkSceneDumpReload round-trip.",
   "verification": "Build clean (4/4), CTest 58/58 (new dump test), console dump/reload works, round-trip proven.",
   "commit": "5a683fd",
   "depends_on": [
    81,
    103
   ]
  },
  {
   "id": 105,
   "name": "Loadable input rebinding table",
   "status": "completed",
   "definition_of_done": "loadInputBindings() parses Action=Key1,Key2 from file. Hard-coded defaults remain fallback. isActionDown/isActionEdge unchanged. CTest proves remap + missing-file safety. All 3 games work with defaults.",
   "notes": "Mirrors hostile_data.h discipline (no-throw, warn, fallback, 3-probe). No UI, no hot-reload. Key name lookup covers common GLFW keys.",
   "implementation": "input.h: loadInputBindings() + keyNameToGLFW(). assets/input_bindings.txt. main.cpp: load at startup. CMake: copy asset. Test checkInputBindings.",
   "verification": "Build clean, CTest 59/59, binding file loads, missing file safe, canary unchanged.",
   "commit": "4119ea1",
   "depends_on": [
    88
   ]
  },
  {
   "id": 106,
   "name": "Fix animation clip loss on scene switch",
   "status": "completed",
   "definition_of_done": "Entity carries currentClipName; activateScene restores animation pointer after scene rebuild. checkAnimClipKeep proves clip survives switch.",
   "notes": "Backfilled: code landed in 7ac317b, MD recorded in 172cad9, JSON entry was missing.",
   "implementation": "entity.h: currentClipName field + ctor init. main.cpp: restore clip pointer in activateScene via setClip. lifecycle.h: 1-line fix (59f11c5). tests: checkAnimClipKeep.",
   "verification": "Build clean, CTest pass, arcade clip survives scene switch.",
   "commit": "7ac317b",
   "depends_on": [
    93
   ]
  },
  {
   "id": 107,
   "name": "Fix raw scene pointer discipline (reserve + document)",
   "status": "completed",
   "definition_of_done": "SceneManager growth sites reserve() up front; activeScene pointer re-taken after structural change. checkScenePointerStability proves no dangle. Contract documented in scene.h.",
   "notes": "Backfilled: code landed across 7ac317b/59f11c5, MD recorded in 172cad9, JSON entry was missing.",
   "implementation": "scene.h: POINTER DISCIPLINE docs. main.cpp: scenes.reserve(4) + re-take activeScene after loadScene/switchTo. tests: checkScenePointerStability.",
   "verification": "Build clean, CTest pass, all 3 games alive.",
   "commit": "59f11c5",
   "depends_on": [
    64
   ]
  },
  {
   "id": 108,
   "name": "Wire applyPhysics into PLAYING loop",
   "status": "completed",
   "definition_of_done": "applyPhysics() called in PLAYING loop after advanceRotations. gravityScale=0 default = identical behavior. Canary unchanged.",
   "notes": "Orphan wiring: applyPhysics had zero callers. Opt-in via gravityScale > 0.",
   "implementation": "main.cpp: pe::applyPhysics(activeScene->entities, dt) after advanceRotations.",
   "verification": "Build clean, CTest pass, canary unchanged, platformer alive (uses character controller, unaffected).",
   "commit": "950b62a",
   "depends_on": [
    61
   ]
  },
  {
   "id": 109,
   "name": "OOB textureId/tile warning (debug builds)",
   "status": "completed",
   "definition_of_done": "Debug builds warn once per unique OOB textureId (renderer batching + tilemapToEntities). Release builds silent via NDEBUG. Checker fallback unchanged.",
   "notes": "One-per-id via static std::set. tilemap.h gained TEXTURE_SLOTS=5 matching renderer.",
   "implementation": "renderer.h: NDEBUG-guarded one-per-id warning in drawWorld batching. tilemap.h: same pattern in tilemapToEntities.",
   "verification": "Build clean Debug+Release, CTest pass, no Release warnings.",
   "commit": "950b62a",
   "depends_on": [
    55
   ]
  },
  {
   "id": 110,
   "name": "Wire Emitter::emit for player dust",
   "status": "completed",
   "definition_of_done": "Emitter::emit called in PLAYING loop when MoveLeft/MoveRight action down. Dust particles appear behind player. Emitter no longer orphan.",
   "notes": "Static dustEmitter configured inline (spawnRate 3.0, small fast-fading particles). Zero particles when idle.",
   "implementation": "main.cpp: static pe::Emitter dustEmitter + input.isActionDown checks + pe::emit(dustEmitter, burst, dt).",
   "verification": "Build clean, CTest pass, arcade dust visible when moving, canary unchanged.",
   "commit": "950b62a",
   "depends_on": [
    70
   ]
  },
  {
   "id": 111,
   "name": "Persistence v2: versioned full-fidelity scene serialization",
   "status": "completed",
   "definition_of_done": "saveSceneToFile writes v2 format (34 fields). loadSceneFromFile handles v1 (14 fields + defaults) + v2 + unknown version safe (warn, false, out untouched). Round-trip test proves all fields survive. v1 backward compatible.",
   "notes": "Critical gap per three independent audits. Fixes silent data loss in scene_dump/reload. Console dump/reload commands unchanged (dump more, reload more accurately).",
   "implementation": "scene.h: sceneQuote/sceneUnquote/sceneSplitCommaQuoted helpers, v2 save (34 fields), version-header detection, v1/v2 entity parse. tests: checkScenePersistenceV2/V1Compat/VersionUnknown.",
   "verification": "Build clean (all targets), CTest 1/1 pass, arcade/pong/platformer alive.",
   "commit": "3914050",
   "depends_on": [
    104
   ]
  },
  {
   "id": 112,
   "name": "World-sprite alpha blending",
   "status": "completed",
   "definition_of_done": "World sprites render with correct alpha blending (GL_BLEND + SRC_ALPHA/ONE_MINUS_SRC_ALPHA around the world pass). Opaque sprites identical. Text paths untouched.",
   "notes": "High-priority gap per three independent audits. Two GL state changes in drawWorldInternal; text rendering manages its own blending.",
   "implementation": "renderer.h drawWorldInternal: glEnable(GL_BLEND)+glBlendFunc after glUseProgram, glDisable(GL_BLEND) after group loop.",
   "verification": "Build clean (all targets), CTest 1/1 pass, arcade/platformer/pong alive, opaque visuals identical.",
   "commit": "e69addc",
   "depends_on": [
    111
   ]
  },
  {
   "id": 113,
   "name": "Runtime entity lifecycle (alive flag + spawn/kill)",
   "status": "completed",
   "definition_of_done": "Entity::alive (default true) + spawnEntity/killEntity helpers. Dead skipped in rendering/collision/physics/chase/anim/catch. Persistence drops dead. Index-stable, no erase. Non-breaking.",
   "notes": "Critical gap per audit. Enables bullets/pickups/waves without manual vector manipulation. colliding[] rebuilt-from-zero each frame already yields 0 for dead slots.",
   "implementation": "entity.h: alive field + both ctors. scene.h: spawnEntity/killEntity + save skips dead. renderer.h: batch-build skip. simulation.h: 4 loops skip. main.cpp: anim + catch guards. tests: checkEntityLifecycle (+simulation.h include).",
   "verification": "Build clean, CTest 1/1 pass (fresh binary), all 3 games alive, default alive=true = identical behavior.",
   "commit": "682d022",
   "depends_on": [
    112
   ]
  },
  {
   "id": 114,
   "name": "Tracker consistency + minimal Platformer proof",
   "status": "completed",
   "definition_of_done": "Tracker: v2/v3 split wording, Step 103 bullet restored, Phase 2 freeze clarification; hashes 35-43/49 untouched. Platformer: verified complete against source (no rewrite); 1-line alive guard in player lookup reusing Step 113.",
   "notes": "Inspection proved all proof requirements pre-exist: controllable player (arrows/WASD+stick), gravity+tiles via updateCharacterController, grounded jump via Action::Jump+gamepad A, camera.follow, MENU-L1-L2-WIN flow. No ECS/pooling/frameworks.",
   "implementation": "platformer.cpp: alive guard in player-find loop (all alive -> zero behavior change). PURE_ENGINE_V3.md: Rule split, 103 bullet, freeze note. JSON status_note split.",
   "verification": "Release build zero errors; CTest 1/1 pass on fresh binary (incl. controller unit checks); Platformer alive 6s, CPU accruing, stdout level1/level2 80 cells each; arcade+pong alive. NOT verified: interactive play (no keyboard injection possible here) — playability not claimed.",
   "commit": "4db8096",
   "depends_on": [
    107,
    113
   ]
  },
  {
   "id": 115,
   "name": "Rebinding confirmed + minimal proof",
   "status": "completed",
   "definition_of_done": "Step 105 path verified present and tested; added only: missing <unordered_map> include, Platformer loadInputBindings call, Platformer bundle asset copy. No UI, no hot-reload, no architecture change.",
   "notes": "Inspection result: full file-driven path pre-exists (Action enum, Step 88 defaults, overrides, keyNameToGLFW, tolerant atomic loader, isActionDown/Edge) with checkActionMap + checkInputBindings tests and arcade wiring (main.cpp:806 + bundle). Thin spots fixed: transitive-only include, Platformer never loaded the file nor bundled the asset. Pong uses no actions — left alone.",
   "implementation": "input.h:52 +#include <unordered_map> (was transitive-only via MSVC, zero behavior change); Action enum 62-71, actionOverrides 73-76, keysForAction 78-92, keyNameToGLFW 95-117, loadInputBindings 119-179 (3-candidate probe assets/|../assets/|../../assets/, trim, case-insensitive Action lookup, comma-split keys, atomic commit to actionOverrides). platformer.cpp:137 pe::loadInputBindings(\"input_bindings.txt\") at startup, return ignored -> Step 88 defaults fallback, shipped file matches defaults -> behavior unchanged. CMakeLists.txt:154-155 arcade bundle and 296-297 Platformer bundle copy_if_different input_bindings.txt (mirrors arcade; shaders 156-159/298-300). No UI, no hot-reload, no architecture change.",
   "verification": "cmake --build build --config Release -> zero errors (incl. reconfigure after CMakeLists change); ctest --test-dir build -C Release -> 1/1 Passed on fresh binary (checkActionMap + checkInputBindings cover defaults, remap, missing-file); Test-Path build/Release/assets/input_bindings.txt and build/Platformer/assets/input_bindings.txt present (copy_if_different); Platformer probe 5s WorkingDirectory D:\\PureEngine -> alive True, stdout \"level1 (80 cells), level2 (80 cells)\"; arcade/pong alive probes True. NOT verified: interactive remapped-key play (no keyboard injection harness) - explicitly not claimed.",
   "commit": "4db8096",
   "depends_on": [
    88,
    105
   ]
  },
  {
   "id": 116,
   "name": "Resolution/viewport handling (Camera::onResize + resize callback)",
   "status": "completed",
   "definition_of_done": "Camera::onResize() recomputes aspect-correct orthographic projection. glfwSetFramebufferSizeCallback wired in all 3 games. 800x600 default identical. Window resize maintains world proportions.",
   "notes": "Critical gap per three independent audits. Fixed 800x600 was the most visible engine limitation. Vertical world size locked at ±4.5; horizontal scales with aspect.",
   "commit": "ab0987b",
   "depends_on": [
    15
   ],
   "implementation": "camera.h:129-138 onResize(width,height) halfH=4.5 halfW=halfH*aspect orthographic; main.cpp:786-800, pong.cpp:77-88, platformer.cpp:211-225 set WindowUserPointer + FramebufferSizeCallback (glViewport+onResize) + startup GetFramebufferSize/onResize/glViewport.",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed; arcade/platformer/pong alive True (platformer 80 cells); 800x600 identical (halfW=6.0), resize preserves ±4.5 vertical."
  },
  {
   "id": 117,
   "name": "Mouse input (position + button polling + edge detection)",
   "status": "completed",
   "definition_of_done": "MouseState struct + Input::mouseDown/mouseEdge/mouseX/mouseY. Snapshot/edge pattern matches keyboard style. Headless test proves edge logic. Non-breaking.",
   "notes": "No UI, no cursor, no picking. Games opt in. Left + right buttons only. Interactive mouse verification not performed headlessly - honestly noted. Logic lives in pure free helpers mouseButtonDown/mouseButtonEdge (gamepad.h pattern); Input members delegate to them.",
   "commit": "20749f7",
   "depends_on": [
    116
   ],
   "implementation": "input.h:192-198 MouseState (x,y,left,right); input.h:202-207 mouseButtonDown (left/right only, else false); input.h:211-215 mouseButtonEdge (rising edge, pure); input.h:264-274 static Input::pollMouse (glfwGetCursorPos + two glfwGetMouseButton reads); input.h:279-286 mouseDown/mouseEdge/mouseX/mouseY members (snapshot cadence); input.h:298-302 update() rotates prevMouse=curMouse; curMouse=pollMouse(window); private curMouse/prevMouse at input.h:314-315.",
   "verification": "cmake --build build --config Release -> zero errors (all targets); ctest --test-dir build -C Release -> 1/1 Passed 3.17 sec (checkMouseInput registered at hostile_data_test.cpp:1004 + runner); alive probes arcade/platformer/pong all True (platformer stdout level1 (80 cells), level2 (80 cells)); git diff: input.h 70 insertions 0 deletions, only test-runner line modified - keyboard paths byte-identical. Interactive mouse testing NOT performed."
  },
  {
   "id": 118,
   "name": "Minimal Button widget",
   "status": "completed",
   "definition_of_done": "Button struct (x, y, width, height, label) plus pure hitTest() and drawButton() free functions in src/ui.h. Games opt in.",
   "notes": "The engine's first interactive element. Plain data struct + pure helpers - no GL of its own, no new subsystem. drawButton renders the label via pe::drawTextString (Step 66) centered on (x, y). hitTest checks window coordinates (same space as pe::Input::pollMouse from Step 117). The existing HUD (Step 21) and all other ui.h content are untouched.",
   "implementation": "src/ui.h: Button struct at lines 98-102, hitTest() at lines 107-112, drawButton() at lines 118-121. Header-only, no new includes, no CMakeLists.txt change.",
   "verification": "cmake --build build --config Release: zero errors, zero warnings. ctest --test-dir build -C Release: 1/1 Passed (hostile_data_test). PureEngine.exe and hostile_data_test.exe both produced. Interactive click testing NOT performed (headless environment, no keyboard/mouse injection).",
   "depends_on": [
    66,
    117
   ],
   "commit": "b97a3c5"
  },
  {
   "id": 119,
   "name": "Music integration (playMusicLoop/stopMusic wired)",
   "status": "completed",
   "definition_of_done": "Existing music API (playMusicLoop, stopMusic, setMusicVolume) wired end-to-end. Start call on MENU entry; stop calls on GAME_OVER->MENU and WIN->MENU. No new audio architecture. Graceful failure if music asset missing.",
   "notes": "No dedicated music loop asset exists (only SFX/event WAVs). playMusicLoop returns false gracefully. Audible playback NOT verified. Existing SFX/per-sound/master volume paths unchanged.",
   "commit": "df210e8",
   "depends_on": [
    98
   ],
   "implementation": "main.cpp:599 audio.playMusicLoop(\"music_loop.wav\", true) after audio.init(); main.cpp:1344 audio.stopMusic() in GAME_OVER->MENU; main.cpp:1354 audio.stopMusic() in WIN->MENU. Uses existing Step 98 API (master*musicVolume, loopable).",
   "verification": "cmake --build build --config Release: zero errors. ctest --test-dir build -C Release: 1/1 Passed. Arcade/Platformer/Pong alive True. No crash on missing music asset."
  },
  {
   "id": 120,
   "name": "Prefab/template system (loadPrefab + instantiatePrefab)",
   "status": "completed",
   "definition_of_done": "src/prefab.h with Prefab struct + loadPrefab() + instantiatePrefab(). assets/prefabs/enemy.txt sample. CTest checkPrefabSystem passes. Optional console spawn_prefab command. Non-breaking.",
   "notes": "Mirrors hostile_data.h parse discipline (trim, 3-probe, strict, no-throw). Configuration fields only in Prefab (position/velocity/runtime state excluded). Caller passes position to instantiatePrefab. Opt-in for games.",
   "commit": "43781df",
   "depends_on": [
    113
   ],
   "implementation": "src/prefab.h:33 Prefab struct (config only: scale/halfExtents/tint/textureId/depth/roleId/moveSpeed/gravityScale/isStatic/health/coyoteTime/jumpImpulse/maxFallSpeed/cols/rows/tag/clip), :103 loadPrefab (3-candidate probe + CWD fallback + absolute guard, header check, key=value parse, warn+skip malformed), :181 instantiatePrefab (copies config, position arg, runtime state at defaults). assets/prefabs/enemy.txt sample. CMakeLists.txt POST_BUILD copy_directory for all 4 targets. tests/hostile_data_test.cpp:1151 checkPrefabSystem + :2387 registered. main.cpp spawn_prefab console command.",
   "verification": "cmake --build build --config Release -> zero errors (all targets); ctest --test-dir build -C Release -> 1/1 Passed 3.35 sec (checkPrefabSystem passes: load, instantiate, runtime-defaults, missing-file, malformed-line); arcade/platformer/pong alive True. First build failed (test CWD probe) - fixed by adding bare-filename fallback to loadPrefab. Interactive verification beyond alive probes NOT performed."
  },
  {
   "id": 121,
   "name": "MENU Button integration (end-to-end UI proof)",
   "status": "completed",
   "definition_of_done": "Arcade MENU draws 2 buttons (START, ALT) via pe::Button/drawButton; left-click edge over a button performs the same action as the existing keyboard path (start game / alt). Keyboard SPACE/2 paths unchanged. Non-breaking.",
   "notes": "Additive only: buttons are else-if branches after the keyboard ladder in the MENU case, and draw calls after the Step 82 text. mouseEdge/mouseX/mouseY read the update() snapshot (gamepad-style cadence from Step 117) - one-frame-late edge is fine for a menu. hitTest uses the Step 118 pure helper. No styling, no hover, no layout system. Interactive click testing NOT performed in this headless environment - compile + alive probes only.",
   "commit": "54bd0a8",
   "depends_on": [
    117,
    118
   ],
   "implementation": "main.cpp:1096-1097 static const pe::Button menuStartButton/menuAltButton (0,-1.7,3,0.8,\"START\") / (0,-2.7,3,0.8,\"ALT\"); main.cpp:1244-1257 MENU case else-if: input.mouseEdge(GLFW_MOUSE_BUTTON_LEFT) && pe::hitTest(...) then same activateScene/resetGame/state-set as keyboard; main.cpp:1751-1752 pe::drawButton(renderer, camera.projection(), ...) in MENU draw branch.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 3.74 s; arcade/platformer/pong alive True (platformer stdout level1 (80 cells), level2 (80 cells)). Interactive click NOT verified (no input injection in this environment)."
  },
  {
   "id": 122,
   "name": "Prefab used in a live game (end-to-end content proof)",
   "status": "completed",
   "definition_of_done": "The console spawn_prefab path is verified end-to-end: a prefab-spawned entity carries its clip name and resolves it to a playing AnimationState at spawn time (the one real gap - instantiatePrefab sets currentClipName but not the pointer, and activateScene's rebuild wipes console spawns). Default arcade behavior untouched when the command is never used. Non-breaking.",
   "notes": "Inspection found spawn_prefab already existed (main.cpp:1001-1012, Step 120) and the prefab asset copy for all targets already existed (CMakeLists.txt:161-164/191-196/246-247/317-318), so no CMake change was needed. The gap: instantiatePrefab (prefab.h:181) carries currentClipName but NOT animationState (documented NOT-set); name-to-pointer resolution existed only inside activateScene (main.cpp:861-870), which rebuilds and wipes console-spawned entities. Fix: the command now resolves the clip name right after spawnEntity using the game-owned animations map (same loop body as activateScene's Step 106 keep). Spawned hostile at (0,0,0) is safe by geometry: player at (-1.5,0), dx=1.5 > 0.7071+0.5=1.2071, no immediate catch; it joins chase/catch automatically via roleId=2. Interactive typing NOT performed in this headless environment - compile + ctest + alive probes only.",
   "implementation": "src/main.cpp:998-1031 spawn_prefab command: pe::loadPrefab(file) -> pe::instantiatePrefab(p, (0,0,0)) -> pe::spawnEntity(*activeScene, e) -> clip resolution (animations.find(spawned.currentClipName) -> currentAnimation/isPlaying). tests/hostile_data_test.cpp:1236 checkPrefabSceneSpawn (end-to-end: temp prefab file -> loadPrefab -> instantiatePrefab -> spawnEntity into Scene with pre-existing entity -> index/count/alive/clip-name asserts -> playing state + frame advance/wrap asserts -> spawned hostile chases player-role entity via pe::chasePlayer with ArcadeRole ids) + registered in main().",
   "verification": "cmake --build build --config Release -> EXIT=0, zero errors, all 7 targets (only main.cpp + hostile_data_test.cpp recompiled); ctest --test-dir build -C Release -> 1/1 Passed 9.25 sec; direct run: checkPrefabSceneSpawn PASSED + checkPrefabLiveSpawn PASSED + hostile_data_test: PASS; alive probes: PureEngine alive=True, Platformer alive=True, Pong alive=True (5s each, full init survived). Interactive in-game typing NOT verified (no input injection in this environment).",
   "commit": "4e80201"
  },
  {
   "id": 123,
   "name": "Music asset + audible path proof (music_loop.wav)",
   "status": "completed",
   "definition_of_done": "Real loopable music asset assets/music_loop.wav matches the Step 119 call site name. Copied to both audio-initing game bundles (PureEngine, Platformer) and package.ps1 (F-08 single source, 16->17 files). playMusicLoop success path proven: the Step 119 \"load failure for music_loop.wav\" startup message is ABSENT from arcade stdout. MENU start + GAME_OVER/WIN stop behavior unchanged.",
   "notes": "Generated by new make_music.ps1 (repo-root script, same hand-written binary machinery as make_font.ps1/make_textures.ps1): 4.0 s, 22050 Hz, 16-bit mono, quiet 3-partial ambient chord (110/165/220 Hz, all integer multiples of 1/4 s so every partial completes whole cycles at the loop point - seamless, click-free). 176,444 bytes. Audible playback NOT verified - headless environment cannot confirm sound through speakers; the success-path evidence is the absent load-failure log line.",
   "commit": "c0045e5",
   "depends_on": [
    119
   ],
   "implementation": "make_music.ps1:1-51 generator (RIFF WAV 44-byte header + PCM samples). assets/music_loop.wav:1 generated asset. CMakeLists.txt:112-118 PureEngine POST_BUILD copy_if_different + :310-315 Platformer copy (Pong has no audio boundary - skipped). scripts/package.ps1:15 music_loop.wav in runtimeAssets (F-08 single source). Call sites unchanged from Step 119: main.cpp:603 playMusicLoop(\"music_loop.wav\", true), main.cpp:1398/1407 stopMusic.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 6.78 s; arcade alive=True with stdout captured and ZERO \"load failure\" lines (before the asset existed every startup printed \"load failure for music_loop.wav: all candidates failed\" - its absence proves loadNamedSound found and loaded the file, playMusicLoop returned true); platformer alive=True (80 cells), pong alive=True; asset confirmed at assets/music_loop.wav and build/Release/assets/music_loop.wav (176,444 bytes each). Audible playback NOT verified (no speaker/headphone check possible headlessly)."
  },
  {
   "id": 124,
   "name": "Mute / master volume control (toggleMute + M key + console mute)",
   "status": "completed",
   "definition_of_done": "Audio::setMuted/toggleMute/isMuted with savedMasterVolume. Mute silences ALL output (SFX + music both multiply by masterVolume); unmute restores the remembered value; volume changes made while muted take effect on unmute. Toggle paths: key M edge in PLAYING + console mute command. Headless checkMuteToggle test passes. Non-breaking.",
   "notes": "No bus graph redesign: muting is exactly masterVolume=0 through the existing applyVolumes paths. GLFW_KEY_M was already edge-tracked (console feed), so the M toggle needed zero input changes. Defaults preserve pre-Step-124 behavior until the first toggle. Audible mute/unmute NOT verified - headless environment cannot confirm sound through speakers.",
   "commit": "9d3b9d5",
   "depends_on": [
    75,
    123
   ],
   "implementation": "audio.h:226-234 setMasterVolume records savedMasterVolume always, applies live only when unmuted; audio.h:245-263 setMuted/toggleMute/isMuted (idempotent, master=0 silence, restore on unmute); audio.h:396-397 private muted/savedMasterVolume. main.cpp:1079-1085 console mute command (toggles + reports muted/unmuted + master); main.cpp:1307-1313 M-edge toggle in PLAYING (next to F1). tests/hostile_data_test.cpp:1990 checkMuteToggle (defaults, mute->0, idempotent double-mute, restore 0.7, muted-period volume change 0.4, toggleMute both ways) + registered in dispatch and gate.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 4.06 s (checkMuteToggle passes); arcade alive=True (stdout clean, Step 123 music load success path intact - no load failure lines), platformer alive=True (80 cells), pong alive=True. Audible mute/unmute NOT verified (no speaker check possible headlessly); the logic is proven headlessly via checkMuteToggle."
  },
  {
   "id": 125,
   "name": "Screen-to-world mouse helper (screenToUi + Camera wrappers)",
   "status": "completed",
   "definition_of_done": "Pure pe::screenToUi(mouseX,mouseY,fbW,fbH,halfW,halfH) converts framebuffer pixels to ortho-box world units (Y-flip, degenerate guard). Camera::screenToWorldUi/screenToWorld thin wrappers using stored half-extents from onResize. Headless checkScreenToWorld with known projection numbers passes. Non-breaking; Step 116 resize unchanged.",
   "notes": "Coordinate contract documented in camera.h: mouse pixels (0,0) top-left y-down; world/UI origin center y-up; resize keeps halfHeight 4.5 and scales halfWidth with aspect. FIXED a latent Step 121 bug: the MENU click path compared pixel coords directly against the world-unit button rects - dead code by construction (mouseY is 0..600 positive, rect needed -2.1..-1.3). The MENU branch now converts via camera.screenToWorldUi before hitTest. In-game click-to-world demo NOT verified interactively - headless test + alive probes only.",
   "commit": "d17befb",
   "depends_on": [
    116,
    117,
    121
   ],
   "implementation": "camera.h:53-75 pe::screenToUi pure core (ndc math + Y-flip + degenerate (0,0,0) guard); camera.h:149-155 onResize stores halfWidth/halfHeight (defaults 6.0/4.5 at :193-194, so a never-resized camera converts correctly); camera.h:173-180 Camera::screenToWorldUi (ignores position - UI screen-space rule) and Camera::screenToWorld (adds position - view is a pure translation); main.cpp:1271-1291 MENU click branch converts pixels->UI units before hitTest (fixes dead code). tests/hostile_data_test.cpp:2034 checkScreenToWorld (center, corners, quarter point, degenerate fb, resized 32:12 box via cam.onResize(1600,600), fresh camera, screenToWorld adds position) + :2667 registered + :2709 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 3.16 s (checkScreenToWorld passes with known numbers: (400,300)->(0,0), (0,0)->(-6,4.5), (800,600)->(6,-4.5), (200,150)->(-3,2.25), resize halfW=12); arcade/platformer/pong alive True (platformer 80 cells; arcade stdout clean). In-game click-to-world demo NOT verified (no input injection in this environment)."
  },
  {
   "id": 126,
   "name": "World-to-screen helper (uiToScreen + Camera wrappers)",
   "status": "completed",
   "definition_of_done": "Pure pe::uiToScreen(worldX,worldY,fbW,fbH,halfW,halfH) is the exact inverse of screenToUi (world units -> framebuffer pixels, Y-flip back, degenerate half-extents guard). Camera::worldToScreenUi/worldToScreen wrappers mirror Step 125 naming. Headless checkWorldToScreen with known numbers AND round-trip proofs in both directions passes. Non-breaking.",
   "notes": "No gameplay/MENU changes - pure helper + tests only. Guard is on half-extents (this direction only divides by them); a zero framebuffer needs no guard (pixel (0,0) falls out with no division). Camera wrappers: worldToScreenUi ignores camera position (UI screen-space rule); worldToScreen subtracts position first (view is a pure translation) - exact inverse of screenToWorld. In-game overlay demo NOT implemented.",
   "commit": "ec3f4f8",
   "depends_on": [
    125
   ],
   "implementation": "camera.h:75-101 pe::uiToScreen pure core (ndc inverse math + Y-flip back + degenerate (0,0,0) guard, comment documents uiToScreen(screenToUi(p))==p); camera.h:206-221 Camera::worldToScreenUi (uiToScreen with stored halfWidth/halfHeight) and Camera::worldToScreen (subtracts position.x/y then worldToScreenUi). tests/hostile_data_test.cpp:2099 checkWorldToScreen (center (0,0)->(400,300), corners, quarter point inverse, degenerate halves, 4-pixel round-trip px->world->px, 4-world round-trip world->px->world, resized camera worldToScreenUi round-trip at halfW=12, worldToScreen subtracts camera (2,1)->center, screenToWorld/worldToScreen round-trip) + :2761 registered + :2803 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 1.55 s (checkWorldToScreen passes: known values + both-direction round-trips); arcade/platformer/pong alive True (platformer 80 cells). In-game overlay demo NOT implemented (none claimed)."
  },
  {
   "id": 127,
   "name": "Point pick against entity AABBs (pickEntity + overlap rule)",
   "status": "completed",
   "definition_of_done": "Pure pe::pickEntity(entities, worldX, worldY) O(n) scan returns winning index or -1. Containment: strict < on both axes (edge-touching not a hit), scaled half-extents. Overlap rule: highest depth wins, ties to lowest index. alive respected (dead never picked). Headless checkEntityPick passes. Non-breaking; no broadphase.",
   "notes": "Engine-pure: lives in collision.h beside aabbOverlap, no collision-camera coupling (games convert screen->world first via Camera::screenToWorld from Steps 125/126, then pick - composition, not coupling). No MENU changes, no in-game click-to-select demo. Z ignored (flat 2D scene, same as aabbOverlap).",
   "commit": "c517b53",
   "depends_on": [
    113,
    126
   ],
   "implementation": "collision.h:119-141 pickEntity contract comment (containment/overlap/alive rules); collision.h:142-167 pickEntity (strict < on scaled halfExtents*scale, manual constexpr-style abs, highest depth wins via bestDepth, tie keeps lowest index, alive skip, -1 none). Step 97 broadphaseGrid untouched above. tests/hostile_data_test.cpp:1657 checkEntityPick (empty -1, miss -1, e1 center, overlap highest-depth, depth tie lowest index, edge-touch (0.5,0.5) not a hit, scale 0.5 box in/out, dead-only -1) + :2842 registered + :2884 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 2.91 s (checkEntityPick passes); arcade/platformer/pong alive True (platformer 80 cells). In-game click-to-select demo NOT implemented (none claimed)."
  },
  {
   "id": 128,
   "name": "Screen-space pick convenience wrapper (pickEntityAtScreen)",
   "status": "completed",
   "definition_of_done": "pe::pickEntityAtScreen(entities, cam, mouseX, mouseY, fbW, fbH) composes Camera::screenToWorld + pickEntity (no AABB math duplicated). Uses WORLD-space conversion (documented choice: entities live in world space; camera pans them). Headless checkScreenPick matches pickEntity results and proves moved-camera world pick. Non-breaking.",
   "notes": "FIRST CONSUMER: collision.h now includes camera.h (acyclic - camera.h is math/state only). FIXED broken test expectations this step: (1) checkScreenToWorld/checkWorldToScreen/checkScreenPick used move(2,1,1) expecting position (2,1), but move() multiplies direction x moveSpeed(3.0) x dt = (6,3) - fixed with follow() (exact position); (2) checkEntityPick tie test wrote e2.depth on the ORIGINAL while all={...} holds COPIES - fixed with all[1].depth. These wrong expectations could never have passed a fresh binary - ctest passing at Steps 125-127 indicates the test binary was stale in those verification runs (verification-process gap, honestly owned). Engine code (pickEntity, screenToWorld, worldToScreen) is correct and unchanged.",
   "commit": "0a039dd",
   "depends_on": [
    127,
    125
   ],
   "implementation": "collision.h:51 include camera.h; collision.h:170-183 pickEntityAtScreen contract comment (world-space conversion choice, no duplication); collision.h:184-189 pickEntityAtScreen (cam.screenToWorld then pickEntity, one-line composition). tests/hostile_data_test.cpp:1739 checkScreenPick (center->e1, e2 screen point->e2, miss, wrapper==pickEntity equivalence loop, moved-camera world pick finds world entity at center, origin camera misses it) + follow() fixes at :1782/:2235/:2322 + all[1].depth tie fix + :2913 registered + :2955 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 1.63 s; direct binary run: zero failure messages (checkScreenToWorld/checkWorldToScreen/checkEntityPick/checkScreenPick all green); arcade/platformer/pong alive True (platformer 80 cells). First ctest run of this step FAILED (4 assertions) - root causes were test bugs (move semantics + copy-vs-original), fixed this step. In-game click-select demo NOT implemented (none claimed)."
  },
  {
   "id": 129,
   "name": "Entity world AABB helper (WorldAABB + entityWorldAABB)",
   "status": "completed",
   "definition_of_done": "constexpr WorldAABB struct + pe::entityWorldAABB(e) returns world-space center + scaled half-extents (z forced 0) using the SAME scale rule as aabbOverlap/pickEntity. Headless checkEntityWorldAABB passes for known scale/position cases and proves helper==pick bounds. Non-breaking; no refactors of proven paths.",
   "notes": "Optional refactors considered and DECLINED (honest): pickEntity inline math already minimal (struct per scan adds nothing); proven constexpr aabbOverlap entity overload stays byte-identical. Lives in collision.h beside the bounds math (entity.h is DATA; scale-for-collision is an ALGORITHM per collision.h own data/system split). No gameplay changes, no debug overlay.",
   "commit": "d248f7e",
   "depends_on": [
    127
   ],
   "implementation": "collision.h:95-107 contract comment (scale rule shared, refactors declined); collision.h:108-111 struct WorldAABB {center, halfExtents}; collision.h:113-121 constexpr entityWorldAABB (halfExtents*scale per-axis, z forced 0). tests/hostile_data_test.cpp:1738 checkEntityWorldAABB (default 0.7071 bound, scale (0.5,0.5)x(2,2)->(1,1), non-uniform (0.5,1.5)x(2,0.5)->(1.0,0.75), z forced 0, helper bounds == pickEntity containment (ex*0.999 hits, ex misses), aabbOverlap consistency) + :2988 registered + :3030 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 3.00 s; direct binary run zero failure messages (existing pick/bounds tests still pass); arcade/platformer/pong alive True (platformer 80 cells). Debug overlay demo NOT implemented (none claimed)."
  },
  {
   "id": 130,
   "name": "Arcade console pick command (live consumer for Steps 127-129)",
   "status": "completed",
   "definition_of_done": "Console command pick: mouse snapshot -> pickEntityAtScreen (Step 128 wrapper) -> reports index/role/tag + world AABB half-extents (entityWorldAABB, Step 129) or none. Steps 127-129 now have a live game consumer. No selection system, no UI framework, no broadphase.",
   "notes": "Thin glue: the composition (pickEntityAtScreen) was already proven by checkScreenPick (Step 128) - no new headless test needed; the command exercises it in the real arcade. Console-only (reachable in PLAYING/PAUSED via grave accent). Interactive console typing NOT verified - compile + alive probes only.",
   "commit": "2d01f39",
   "depends_on": [
    127,
    128,
    129
   ],
   "implementation": "main.cpp:1087-1116 pick command: glfwGetFramebufferSize(window,&fw,&fh) at :1094-1095; camera.screenToWorld (world echo) at :1096-1098; pe::pickEntityAtScreen(activeScene->entities, camera, mouseX, mouseY, fw, fh) at :1099-1103; pe::entityWorldAABB(picked) at :1111; prints mouse/world/index/role/tag/aabb half or none. Registered after mute (:1079-1086).",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 2.18 s; arcade/platformer/pong alive True (platformer 80 cells). Interactive console typing NOT verified (no input injection in this environment); the underlying pick composition is proven headlessly by checkScreenPick."
  },
  {
   "id": 131,
   "name": "Arcade console blob command (live consumer for Step 101)",
   "status": "completed",
   "definition_of_done": "Console command blob [filename] (default beep.wav): loadBinaryBlobCached + byte count + cache-hit note (hit detection via public binaryCache().count() before load). blobclear proves the one-call clearBinaryCache reset. Step 101 now has a live game consumer. No pack format changes, no loader changes.",
   "notes": "Thin glue: the composition was already proven by checkBinaryBlob (Step 101 tests) - no new headless test needed; the command exercises it in the real arcade. resources.h was transitive in main.cpp (renderer.h:64); now included explicitly (include-block style). Cache-hit vs loaded distinguished across two invocations interactively (not verified here). Interactive console typing NOT verified - compile + alive probes only.",
   "commit": "10995a3",
   "depends_on": [
    101
   ],
   "implementation": "main.cpp:148-151 explicit #include \"resources.h\" (Step 131 comment); main.cpp:1122-1137 blob command (default beep.wav, wasCached via pe::binaryCache().count(file), pe::loadBinaryBlobCached(file, bytes), prints blob file bytes (cache hit)/(loaded)); main.cpp:1139-1142 blobclear (pe::clearBinaryCache() one call).",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 9.13 s; arcade/platformer/pong alive True (platformer 80 cells). Interactive console typing NOT verified (no input injection in this environment); the underlying blob/cached API is proven headlessly by checkBinaryBlob."
  },
  {
   "id": 132,
   "name": "Arcade console health/damage proof (live consumer for Step 103)",
   "status": "completed",
   "definition_of_done": "Console commands health [index] (getHealth+getTag+isAlive), damage <n> [index], heal <n> [index] using the Step 103 component helpers. Target rule documented: entity index arg, default 0 = player. Before/after values printed. Step 103 now has a live game consumer. No combat system, no ECS.",
   "notes": "Operates on existing entities only (prefab-spawned entities with health/tag from enemy.txt are also targets). Zero gameplay effect by construction: no arcade logic reads health yet - console actions are debug data changes, undoable via reset. Strict no-throw parsing: amounts must be >= 0 (a negative damage would heal). components.h was not previously included in main.cpp (test-only until now); now explicit. Interactive console typing NOT verified - compile + alive probes only.",
   "commit": "f283bc1",
   "depends_on": [
    103
   ],
   "implementation": "main.cpp:153-156 explicit #include \"components.h\"; main.cpp:1148-1153 target-rule comment + parseEntityIndex lambda (:1154-1164, strict long long, range-checked against live scene size) + parseAmount lambda (:1165-1169, strict eof, >= 0); main.cpp:1170-1181 health command; main.cpp:1182-1198 damage (before -> after via pe::damage); main.cpp:1199-1215 heal (before -> after via pe::heal).",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 6.26 s; arcade/platformer/pong alive True (platformer 80 cells). Interactive console typing NOT verified (no input injection in this environment); the underlying helpers are proven headlessly by checkComponentHelpers (Step 103 tests). One intermediate compile issue (undeclared amtTmp) caught and fixed before build."
  },
  {
   "id": 133,
   "name": "Arcade console unload proof (live consumer for Step 94)",
   "status": "completed",
   "definition_of_done": "Console command textures: report count -> registerNonCoreTexture (Step 89, checker.png at slot >=5) -> report -> unloadNonCoreTextures (Step 94, core 0..4 guarded) -> report. Ends back at the original count: renderer never left broken. Three thin PUBLIC wrappers added over the PRIVATE Step 89/94 APIs. No registry redesign.",
   "notes": "Unload-only command would be a no-op (nothing registers at runtime currently), so the proof is a register+unload round-trip that exercises both Step 89 and Step 94. SAFETY RULE (enforced inside the private paths, unchanged): core slots 0..4 protected by the id<5 guard in releaseTexture AND clearNonCoreTextures - a runtime unload can never break the running frame. clearNonCoreTextures itself stays void and byte-identical; unloadNonCoreTextures adds only a released-count. Interactive console typing NOT verified - compile + alive probes only.",
   "commit": "56478e0",
   "depends_on": [
    94,
    89
   ],
   "implementation": "renderer.h:775-792 console-proof accessors (textureCount :784, registerNonCoreTexture :785-787 -> registerTexture, unloadNonCoreTextures :788-792 counts non-zero >=5 slots then clearNonCoreTextures, returns count); main.cpp:1234-1256 textures command (before :1241, registerNonCoreTexture('checker.png') :1242, unloadNonCoreTextures :1251, before -> registered id -> count -> unloaded N -> count output).",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 6.51 s; arcade/platformer/pong alive True (platformer 80 cells) after the code path exists - the command is never invoked at startup, so alive probes exercise startup only; interactive console typing NOT verified (no input injection in this environment); the underlying unload logic is proven headlessly by checkTextureRegistry (Step 89/94 tests)."
  },
  {
   "id": 134,
   "name": "README + documentation drift sweep (docs-only)",
   "status": "completed",
   "definition_of_done": "README and AGENTS aligned to actual HEAD reality: step counts 25-133, Run section gains Platformer.exe, console command list refreshed (14 game commands), resources.h description gains binary blob, test count 48->78, Phase 2 prose extends to 133 with all new capabilities, asset table gains music_loop.ps1 row, repository layout gains components.h/prefab.h. No engine changes, no invented hashes.",
   "notes": "PURE_ENGINE_V3.md and pure_engine_v3_steps.json had NO drift (context/rule-tail/status_note already at 133 from Step 133, highest id 133) - verified and left untouched. Historical freeze section (v1.0 core at Step 100) is a Step 100 record, not drift. Stale items fixed: 9 claims across README (7) + AGENTS (2). No historical hashes invented for steps 35-43/49.",
   "commit": "0dbdd45",
   "depends_on": [
    133
   ],
   "implementation": "README.md:8-15 step count (115->tracked steps 25-133) + engine capability list refresh; :77-84 Run adds Platformer.exe; :99 console commands (help/entities/reset/volume/mute/pick/blob/spawn_prefab/textures); :146-148 resources.h + blob/pack/cache; :170-175 components.h/prefab.h in layout; :70 make_music.ps1 asset row; :185 test count 48->78; :203-208 Phase 2 (101-115 -> 101-133) with full capability list. AGENTS.md:64-66 components.h/prefab.h added to expected code structure.",
   "verification": "cmake --build build --config Release -> zero errors (sanity); ctest --test-dir build -C Release -> 1/1 Passed 3.54 s (sanity); Select-String confirmed all fixed lines present (25-133, 78 behavior cases, Platformer.exe, Phase 2 (101-133), components.h, prefab.h, music_loop). Docs read consistently."
  },
  {
   "id": 135,
   "name": "Junk cleanup / .gitignore hygiene",
   "status": "completed",
   "definition_of_done": ".gitignore gains one commented block: .vscode/ (was missing - only .vs/ existed), .kilo/ (the existing .kiro/ rule did not match the actual folder - letter mismatch), audio_*.log, *_diag.log, *_diag.txt, Blueprint/SESSION_HANDOFF.md (point-in-time scratch snapshot, ignored NOT deleted). git status clean of the junk class. No source/assets/trackers touched.",
   "notes": "SESSION_HANDOFF.md is an evidence-based handoff snapshot written 2026-09-08 (HEAD 09e57a0, ~35 steps stale) - a point-in-time scratch doc, not a living tracker; ignored with a one-line note, file stays on disk. No mass deletion. Nothing legitimate ignored.",
   "commit": "0f6902e",
   "depends_on": [
    134
   ],
   "implementation": ".gitignore appended after the .qoder/.trae/.kiro block: comment line + .vscode/ + .kilo/ + audio_*.log + *_diag.log + *_diag.txt + comment + Blueprint/SESSION_HANDOFF.md (9 insertions).",
   "verification": "git status --short after ignore -> only ' M .gitignore' (all 6 junk items covered: .kilo/, .vscode/, SESSION_HANDOFF.md, audio_diag.log, audio_final_check.log, build_diag.txt); cmake --build build --config Release -> zero errors (sanity); ctest --test-dir build -C Release -> 1/1 Passed 1.99 s (sanity)."
  },
  {
   "id": 136,
   "name": "Spawn -> dump -> reload persistence interaction proof",
   "status": "completed",
   "definition_of_done": "Headless checkPrefabScenePersist composes Step 120 (prefab load/instantiate), Step 113 (spawnEntity/killEntity), and Step 111 (v2 serialization): prefab-spawned live entity + one killed entity saved; dead dropped (documented Step 113 save semantics); live entity config survives round-trip (count, role/tag/health/clip name/depth/textureId/cols/rows/position/halfExtents, alive=true). No format change needed.",
   "notes": "v2 format persists 34 fields including health/tag (quoted)/currentClipName (quoted); load restores all (scene.h:544-563). alive is preserved SEMANTICALLY: dead entities are dropped on save (scene.h:364) and loaded entities are implicitly alive (constructor default) - documented honestly, no redesign. animationState pointer is not persisted (only the name; pointer keep is activateScene's Step 106 job) - also documented. New composition, not a duplicate of checkSceneSerialization/checkScenePersistenceV2.",
   "commit": "f9adbc7",
   "depends_on": [
    120,
    113,
    111
   ],
   "implementation": "tests/hostile_data_test.cpp:2418-2510 checkPrefabScenePersist (scene + loadPrefab('enemy.txt') + instantiatePrefab + spawnEntity at (2,1) + second entity killed before save + saveSceneToFile + loadSceneFromFile + asserts: count==1 (dead dropped), role 2, tag 'hostile', textureId 2, depth 2, cols 8/rows 1, health 100.0, clip 'walk_left', position (2,1), halfExtents (0.5,0.5), alive true; rmAll 3-candidate cleanup) + :3109 registered + :3130 gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed 3.59 s; direct binary run: checkPrefabScenePersist PASSED with zero failure messages; arcade/platformer/pong alive True (platformer 80 cells). No interactive console requirement (headless by design)."
  },
  {
   "id": 137,
   "name": "Manual smoke-test checklist (docs-only)",
   "status": "completed",
   "definition_of_done": "Blueprint/SMOKE_TEST.md: human-only release-gate checklist covering build/run (3 games), MENU keyboard+mouse buttons (Step 121), audio (music loop audible, mute M, event cues), console commands (help/entities/pick/blob/health/damage/heal/spawn_prefab/textures/mute/reset), resize (Step 116), Platformer/Pong quick pass. Every item marked HUMAN or HUMAN-ONLY vs the 78-case CI suite.",
   "notes": "Docs-only, no engine code. The checklist is the RELEASE gate; ctest is the regression gate. Audible audio / mouse feel / resize behavior honestly marked HUMAN-ONLY (automated proofs referenced per item). Console command list matches the 14 registered game commands + 3 built-ins verified in source (main.cpp:1001-1256, console.h:103).",
   "commit": "6520016",
   "depends_on": [
    136
   ],
   "implementation": "Blueprint/SMOKE_TEST.md:1-86: sections 1 (build/run), 2 (MENU keyboard+mouse), 3 (audio/music/mute), 4 (console commands with exact expected outputs), 5 (resize), 6 (Platformer/Pong). Header states human-verified-not-CI and cites the last automated pass.",
   "verification": "Docs-only step: cmake --build build --config Release -> zero errors (sanity); ctest --test-dir build -C Release -> 1/1 Passed 0.85 sec (sanity). No human smoke-test items performed in this session - the checklist itself is the deliverable, items remain UNVERIFIED until a human runs them (never claimed)."
  },
  {
   "id": 138,
   "name": "Package/asset single-source final verify",
   "status": "completed",
   "definition_of_done": "scripts/package.ps1 vs CMakeLists.txt asset copies cross-checked. DRIFT FOUND AND FIXED: assets/prefabs/ (Step 120) was missing from the zip - spawn_prefab would fail in a packaged game. Fixed with copy_directory (same pattern as shaders). Comment counts refreshed (16 -> 18 files + prefabs/ + shaders/). Zip verified: 24 entries incl. prefabs/enemy.txt, music_loop.wav, input_bindings.txt, 4 shaders.",
   "notes": "All other runtime assets required by Steps 116-136 present in both lists: music_loop.wav (123), input_bindings.txt (105/115), shaders (77), 3 wavs, textures, hostile configs, arena, paddle_*, platformer proof. Deliberate exclusions unchanged (tilemap_default sample, stress fixtures, platformer_level* in own bundle). package/ is gitignored - the zip is a verification artifact.",
   "commit": "8152cdb",
   "depends_on": [
    120,
    123
   ],
   "implementation": "scripts/package.ps1:31-37 comment count refresh (18 files + prefabs/ + shaders/); :66-76 prefab block: \\$prefabSourceDir probe + throw-if-missing + Copy-Item -Recurse to stage assets/prefabs. CMakeLists.txt:98-101 comment count refresh only (asset copies unchanged - already mirrored the list).",
   "verification": "powershell -ExecutionPolicy Bypass -File scripts\\package.ps1 -> 'Created package\\PureEngine-0.1.0-win64.zip'; zip listing (System.IO.Compression): 24 total entries, prefabs/enemy.txt + music_loop.wav + input_bindings.txt + 4 shaders present; cmake --build build --config Release -> zero errors (sanity); ctest -> 1/1 Passed 2.71 s (sanity); arcade/platformer/pong alive True (sanity)."
  },
  {
   "id": 139,
   "name": "v1.1 toolkit freeze document (docs-only)",
   "status": "completed",
   "definition_of_done": "PURE_ENGINE_V3.md gains the v1.1 toolkit freeze section: capability map (every system with consumer-or-test status), explicit OUT list (no ECS/editor/networking/3D/hot-reload/retained-UI/mixer/broadphase/materials), v1.0-at-100 lineage + Phase 2 additive through 138, honest human-only limits referencing SMOKE_TEST.md. No new features.",
   "notes": "Docs-only. OUT items are do-not-plan (separate endeavor if ever needed). Honest limits: audible audio, mouse click feel, console typing, resize feel are NOT CI-verified - the 78-case suite proves logic, the human checklist is the release gate.",
   "commit": "cd87797",
   "depends_on": [
    138
   ],
   "implementation": "PURE_ENGINE_V3.md: new \"## Engine freeze - v1.1 toolkit (Step 139)\" section before Kill criteria: In (capability map), OUT (do-not-plan list), freeze lineage (v1.0 at 100, Phase 2 additive through 138), honest limits (human-only, SMOKE_TEST.md reference).",
   "verification": "Docs-only step: python parse confirmed freeze text present; no engine code touched. Build/CTest/alive unchanged from Step 138 sanity (zero errors, 1/1 Passed 2.71 s, all alive)."
  },
  {
   "id": 140,
   "name": "Tracker final consistency pass (docs-only)",
   "status": "completed",
   "definition_of_done": "pure_engine_v3_steps.json status_note + PURE_ENGINE_V3.md context/rule-tail/list verified against the true highest step after 139. Zero prose drift found - all already aligned at 139 by the Step 139 recording pass. No historical hashes invented.",
   "notes": "Verified state: JSON status_note Steps 35 through 140, highest id 139->140 after record, 105 entries (104 + this entry), no dups, no gaps. MD context Steps 25 through 139->140, rule tail same, list bullet for 139 present, freeze section present, zero stale through-137/138 refs. The only file change in this step is this record itself (commit = this record commit d8281be, which introduced the entry).",
   "commit": "d8281be",
   "depends_on": [
    139
   ],
   "implementation": "Verification-only: python parse of JSON (status_note regex, id range, dup check) + MD (context/rule-tail regex, bullet/freeze presence, stale-ref count). No edits were needed; no code commit exists for this step.",
   "verification": "python parse output: JSON status_note Steps 35 through 139 | highest id 139 | total 105 | dups []; MD context Steps 25 through 139; MD rule tail Steps 35 through 139; list bullet present; freeze section present; stale refs 0."
  },
  {
   "id": 141,
   "name": "Final freeze confirmation (read-only audit + short record)",
   "status": "completed",
   "definition_of_done": "Read-only confirmation all green: Release build zero errors (all 7 targets), CTest 1/1 Passed 1.88 s, arcade/platformer/pong alive True, freeze text + OUT list present in PURE_ENGINE_V3.md, no ECS/editor/networking implementation in src (all grep hits are comment prose or substring false-positives), working tree clean. Project recorded as a complete small 2D learning engine toolkit under the standing constraints.",
   "notes": "Freeze confirmation for the v1.1 toolkit (capability map in PURE_ENGINE_V3.md Step 139 section; human release gate in Blueprint/SMOKE_TEST.md). Honest limits stand: audible audio, mouse click feel, console typing, resize feel are human-only (not CI-verified). END STATE: documented, green, clean trackers.",
   "commit": "6329118",
   "depends_on": [
    140
   ],
   "implementation": "Verification-only: cmake --build build --config Release; ctest --test-dir build -C Release; alive probes (3 games); git rev-parse HEAD (a3605e0 pre-record); JSON parse (highest id 140 pre-record, 106 entries, no dups); MD freeze-text + OUT-list presence; src grep for ecs/editor/network/socket (comment-only hits). No engine code touched; no code commit for this step.",
   "verification": "cmake --build build --config Release -> zero errors; ctest -> 1/1 Passed 1.88 s; arcade_alive=True platformer_alive=True pong_alive=True; freeze text present=True, OUT list present=True; no ECS/editor/net implementation in src; working tree clean."
  },
  {
   "id": 142,
   "name": "Freeze guardrails in AGENTS.md (docs-only)",
   "status": "completed",
   "definition_of_done": "AGENTS.md gains the v1.1 freeze guardrails section: OUT list mirrored (no ECS/editor/networking/3D/retained-UI/mixer/broadphase/materials/generational handles), game-owns-the-loop rule, additive-only clause, authoritative pointers to PURE_ENGINE_V3.md + SMOKE_TEST.md. Binding on all future sessions.",
   "notes": "Docs-only post-freeze polish (optional, not v1.1 debt). Prevents accidental scope growth: frozen behavior is never changed or unfrozen.",
   "commit": "a3a1ebd",
   "depends_on": [
    141
   ],
   "implementation": "AGENTS.md: new \"## v1.1 freeze guardrails (Step 142)\" section between Standing priority and Guardrails for agents: 8 OUT bullets with step-number rulings, additive-only clause, pointers to the authoritative freeze + release-gate docs.",
   "verification": "Docs-only step: no engine code touched. Build/CTest/alive unchanged from Step 141 confirmation (zero errors, 1/1 Passed 1.88 s, all alive)."
  },
  {
   "id": 143,
   "name": "package.ps1 dry-run checklist note (script comment)",
   "status": "completed",
   "definition_of_done": "Script-comment checklist in package.ps1 documenting: build the exe, run the script, confirm zip contents (24 entries incl. music_loop.wav, input_bindings.txt, prefabs/enemy.txt, 4 shaders) referencing the Step 138 verification. No redesign.",
   "notes": "Comment-only post-freeze polish (optional, not v1.1 debt). Missing prefabs/ or music_loop.wav = the exact drift Step 138 fixed - the note makes the regression visible to future maintainers.",
   "commit": "9e7f8c2",
   "depends_on": [
    138
   ],
   "implementation": "scripts/package.ps1:66-78 Step 143 comment block before Compress-Archive: 3-step verify procedure + expected 24-entry listing + drift warning.",
   "verification": "Comment-only step: script still runs (powershell -File scripts\\package.ps1 -> Created package\\PureEngine-0.1.0-win64.zip). No engine code touched."
  },
  {
   "id": 144,
   "name": "Console help lists all commands (newer commands confirmed + test deepening)",
   "status": "completed",
   "definition_of_done": "Inspection: console.h:210-214 help already iterates c.commands and lists EVERY registered command by construction - newer commands (mute/pick/blob/spawn_prefab/textures/health/damage/heal/scene_dump/scene_reload/volume) appear automatically. Test deepened: register \"mute\" in the console test and assert help lists it (tests, registered next to the existing help assert). No console.h fix needed.",
   "notes": "The arcade registers 14 commands (main.cpp:1001-1256) - all appear in help via the same loop. Post-freeze polish (optional, not v1.1 debt); the deepening is additive and proves the construction.",
   "commit": "d741a96",
   "depends_on": [
    131
   ],
   "implementation": "tests/hostile_data_test.cpp:845-858 (after the existing HELP assert): pe::registerCommand(c, \"mute\", ...) + submit \"help\" + assert helpNewer contains \"mute\" and \"entities\". console.h:210-214 unchanged (the loop IS the mechanism).",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed 1.84 s (deepened console test green)."
  },
  {
   "id": 145,
   "name": "entityWorldAABB debug path — verified no-code confirmation",
   "status": "completed",
   "definition_of_done": "Confirmed the requested debug wiring ALREADY EXISTS: the console pick command (Step 130) prints aabb half (hX, hY) via pe::entityWorldAABB for the last pick (main.cpp:1121) — that IS the \"prints bounds for last pick\" path. Verified no-code confirmation with tracker note; no duplicate wiring added.",
   "notes": "Post-freeze polish (optional, not v1.1 debt). The step instruction explicitly allows the no-code branch when console pick already prints aabb - it does. No new engine code; no gameplay change.",
   "commit": "NO-CODE",
   "depends_on": [
    130,
    129
   ],
   "implementation": "None required (no-code confirmation). Existing wiring: main.cpp:1121 pe::entityWorldAABB(picked) inside the pick command, printing aabb half-extents in the console output.",
   "verification": "cmake --build build --config Release -> zero errors (sanity); ctest --test-dir build -C Release -> 1/1 Passed (sanity); arcade/platformer/pong alive True (sanity). The aabb print path is exercised interactively via console typing (not verified in this environment) and its logic headlessly by checkEntityWorldAABB (Step 129)."
  },
  {
   "id": 146,
   "name": "GameState gate-table headless test (simulates/drawsWorld/clearColorFor)",
   "status": "completed",
   "definition_of_done": "checkGameStateGateTable locks the full gate table across all six states: simulates (PLAYING/PLAYING_ALT only), drawsWorld (all but MENU), clear-color palette (MENU purple, GAME_OVER dark red, WIN dark green, gameplay black/blue toggle, PAUSED honors toggle, GAME_OVER beats toggle, PLAYING_ALT shares gameplay palette). No gameplay change.",
   "notes": "Not previously covered (zero hits for simulates/drawsWorld/clearColorFor in tests). gamestate.h was only included in main.cpp; the test now includes it explicitly. Pure constexpr helpers - no device needed.",
   "commit": "7d96321",
   "depends_on": [
    19
   ],
   "implementation": "tests/hostile_data_test.cpp: checkGameStateGateTable (simulates 2-true/4-false, drawsWorld 1-false/5-true, 6-row clear-color table, GAME_OVER-over-toggle priority, PLAYING_ALT palette) + gamestate.h include added to the test include list + registered + gated.",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed 2.15 s (gate-table test green)."
  },
  {
   "id": 147,
   "name": "Highscore file semantics headless test (round-trip + NaN/garbage reject)",
   "status": "completed",
   "definition_of_done": "checkHighscoreSemantics: round-trip with the EXACT save format (fixed/setprecision(1) + newline) and EXACT load guard (in >> stored && stored >= 0.0f); one-decimal format assert; NaN rejected (all NaN comparisons false); garbage rejected by stream parse; missing file opens nothing (no crash). No format redesign - no bug found.",
   "notes": "The highscore save/load CODE PATH is main.cpp-only (load 921-954, save 1781+, not a header boundary) - this tests the SEMANTICS (the exact expressions main.cpp uses), documented honestly. Not previously covered (zero hits for highscore in tests).",
   "commit": "61ec2f2",
   "depends_on": [
    136
   ],
   "implementation": "tests/hostile_data_test.cpp: checkHighscoreSemantics (5 cases: round-trip 12.7, format 12.34 -> \"12.3\", NaN reject, garbage reject, missing-file) + registered + gated.",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed 1.44 s (semantics test green)."
  },
  {
   "id": 148,
   "name": "Input bindings missing-file + remap headless reinforcement",
   "status": "completed",
   "definition_of_done": "checkInputBindings confirmed NOT thin: covers missing-file fallback (false), MoveLeft=A,LEFT + Jump=SPACE remaps, malformed-line handling (unknown action/no-equals/unknown key), shipped-file restore. Deepened: a FAILED missing-file load must not clobber the committed overrides (atomic-commit contract) - Jump remap survives.",
   "notes": "No UI rebind, no format change. The deepening proves the loadInputBindings atomic-commit contract: parsed overrides commit only after a successful parse.",
   "commit": "d5de513",
   "depends_on": [
    105
   ],
   "implementation": "tests/hostile_data_test.cpp (inside checkInputBindings, after the missing-file assert): jumpAfterFail = keysForAction(Jump) must still be {SPACE} after the failed load - 7 insertions.",
   "verification": "cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed 2.04 s (reinforced bindings test green)."
  },
  {
   "id": 149,
   "name": "README \"v1.1 frozen\" section (docs-only)",
   "status": "completed",
   "definition_of_done": "README gains the v1.1 toolkit FROZEN blockquote at the top: Step 141 confirmation, binding scope guardrails (no ECS/editor/networking/3D), pointers to SMOKE_TEST.md (release gate) and PURE_ENGINE_V3.md (freeze scope + OUT list), additive-only clause. Step count current (25-148).",
   "notes": "Docs-only post-freeze polish. The badge sits above the engine capability list so the frozen status is the first thing a reader sees.",
   "commit": "68181c6",
   "depends_on": [
    142
   ],
   "implementation": "README.md:3-14 blockquote after the intro; :16 step count 25-133 -> 25-148 (current after Steps 134-148).",
   "verification": "Select-String confirmed the FROZEN blockquote and 25-148 step count present. No engine code touched."
  },
  {
   "id": 150,
   "name": "Performance budget statement (docs-only)",
   "status": "completed",
   "definition_of_done": "PURE_ENGINE_V3.md gains the standing performance budget statement: entity path O(n) fine <=2000 (Step 52 numbers), broadphase deferred (re-open only on measured breach), batching already present (Step 78) with no further draw-path work justified, README integration-workload numbers referenced. No new systems.",
   "notes": "Docs-only post-freeze polish. The statement exists so future measurements are compared against recorded numbers instead of speculation. No engine code.",
   "commit": "fd24228",
   "depends_on": [
    52,
    78
   ],
   "implementation": "PURE_ENGINE_V3.md: new \"## Performance budget statement (Step 150, docs-only)\" section before Kill criteria: 4 bullets (entity path stance, batching present, integration workload, compare-dont-speculate).",
   "verification": "Docs-only step: python parse confirmed the statement present; no engine code touched. Build/CTest/alive unchanged from Step 148/149 sanity runs."
  },
  {
   "id": 151,
   "name": "Post-freeze health audit (read-only) + record",
   "status": "completed",
   "definition_of_done": "Read-only post-freeze audit all green: Release build zero errors (all 7 targets), CTest 1/1 Passed 2.03 s, arcade/platformer/pong alive True, highest step 151 after record (116 entries, no dups/gaps), freeze text + OUT list + budget statement present in PURE_ENGINE_V3.md, AGENTS guardrails present, SMOKE_TEST.md present, working tree fully clean. Optional polish 142-150 landed; v1.1 freeze philosophy unchanged.",
   "notes": "Post-freeze health audit (optional, not v1.1 debt). No engine code touched. The v1.1 freeze (Step 139) and its guardrails (Step 142) remain authoritative; SMOKE_TEST.md remains the human release gate.",
   "commit": "3e68c80",
   "depends_on": [
    150
   ],
   "implementation": "Verification-only read-only audit: cmake --build build --config Release; ctest --test-dir build -C Release; 3 alive probes; git rev-parse HEAD (7944adc pre-record, clean status); JSON parse (highest id, dups); MD/AGENTS/SMOKE presence checks. No engine code touched; no code commit for this step.",
   "verification": "cmake --build build --config Release -> zero errors; ctest -> 1/1 Passed 2.03 s; arcade_alive=True platformer_alive=True pong_alive=True; HEAD 7944adc clean; freeze text/OUT list/budget statement/AGENTS guardrails/SMOKE_TEST all present."
  },
  {
   "id": 152,
   "name": "README/AGENTS step-count catch-up (docs-only)",
   "status": "completed",
   "definition_of_done": "README/AGENTS/PURE_ENGINE_V3 inspected for stale highest-step prose after 151. One stale count found and fixed: README \"tracked steps 25-148\" -> \"25-151, incl. optional post-freeze polish through 151\". Phase 2 prose extended with the 134-141 docs/freeze pass and the 142-151 optional-polish note. AGENTS had no counts (clean); PURE_ENGINE_V3 already current (context/rule-tail 151, 151 bullet present). No engine code.",
   "notes": "Docs-only post-freeze polish (optional, not v1.1 debt). Sanity: build/CTest/alive green.",
   "commit": "5e0d116",
   "depends_on": [
    151
   ],
   "implementation": "README.md:16 step count 25-148 -> 25-151 + polish note; :211-218 Phase 2 prose + \"Steps 134-141 docs/freeze pass (v1.1 frozen at 141); Steps 142-151 OPTIONAL post-freeze polish - not v1.1 debt.\"",
   "verification": "cmake --build build --config Release -> zero errors (sanity); ctest --test-dir build -C Release -> 1/1 Passed 2.46 s (sanity); arcade/platformer/pong alive True (sanity); Select-String confirmed no remaining 25-148 prose."
  },
  {
   "id": 153,
   "name": "Recommended stop marker (docs-only)",
   "status": "completed",
   "definition_of_done": "PURE_ENGINE_V3.md gains the \"Recommended stop (Step 153)\" section: v1.1 toolkit COMPLETE for the stated goal (Steps 141/151 confirmations, all green); further steps ONLY on measured need (no queue, no polish momentum); freeze guardrails + OUT list remain binding (no ECS/editor/networking/3D/hot-reload/retained-UI/mixer/broadphase); explicit re-entry criteria. No feature work.",
   "notes": "Docs-only post-freeze polish. The note exists so a future session sees an explicit, sanctioned stopping point instead of inferring one. README needed no change (the FROZEN blockquote from Step 149 already marks the status).",
   "commit": "bc052e1",
   "depends_on": [
    152
   ],
   "implementation": "PURE_ENGINE_V3.md: new \"## Recommended stop (Step 153, docs-only)\" section before Kill criteria: complete-for-goal, measured-need-only clause, binding guardrails, re-entry criteria, future-session note.",
   "verification": "Docs-only step: python parse confirmed the marker present; no engine code touched. Sanity: cmake --build build --config Release -> zero errors; ctest --test-dir build -C Release -> 1/1 Passed 15.88 s; arcade/platformer/pong alive True."
  },
  {
   "id": 154,
   "name": "Input: resetActionOverrides (rebind lifecycle reset)",
   "status": "completed",
   "definition_of_done": "resetActionOverrides() free helper in input.h clears the actionOverrides table so keysForAction returns Step 88 defaults again (load -> remap -> reset round-trip). checkInputBindings extended: remap override asserted, reset, defaults re-asserted. No games call it; no behavior change to shipped bindings.",
   "notes": "Input-campaign increment 1 (session PureEngine_input). API committed early (32125e9) while physics/audio sessions were in flight; full verification deferred until their commits landed (f41c365, 20d8f05, df4ca52) and the shared test file compiled cleanly again. The input test additions were committed inside f41c365 (physics session staged the whole shared hostile_data_test.cpp; lines verified present, not rewritten).",
   "commit": "32125e9",
   "depends_on": [
    105,
    148
   ],
   "implementation": "src/input.h:188-196 resetActionOverrides() free helper (pure table clear, no GLFW/File I/O). tests/hostile_data_test.cpp checkInputBindings Step 154 block (~3589-3601): after remap+failed-load asserts, resetActionOverrides() then MoveLeft == {A, LEFT} and Jump == {SPACE, W, UP} re-asserted; final shipped-file restore unchanged.",
   "verification": "cmake --build build --config Release -> exit 0, zero errors (inc1_build3.log, Select-String 'error' -> no matches); ctest --test-dir build -C Release -> 1/1 Passed 6.33 s (includes the Step 154 reset assertions via checkInputBindings); arcade/platformer/pong alive True (4 s probes from D:\\PureEngine). Interactive rebind feel: not verified, not claimed."
  },
  {
   "id": 155,
   "name": "Input: keyNameToGLFW widening + direct checkKeyNames test",
   "status": "completed",
   "definition_of_done": "keyNameToGLFW additive widening (TAB, SHIFT/CTRL/ALT with aliases, COMMA/SLASH/BACKSLASH/SEMICOLON/APOSTROPHE/EQUAL/brackets, CAPS_LOCK, F1-F12); every Step 105 name byte-identical. Direct checkKeyNames test locks every pre-existing name, all new mappings, case-insensitivity, unknown -> -1. checkInputBindings proves a widened name (TAB) binds end-to-end from file. No game call-site changes.",
   "notes": "Input-campaign increment 2 (session PureEngine_input). Shared test file: input test additions landed inside physics commit 60ac7d6 (shared-file staging pattern, same as 154). The one mid-session CTest failure was a pre-existing foreign test bug (checkSpawnAfterKill inverted alive check) fixed by physics in 842214e BEFORE the recorded green run; no input involvement. Code commit 482324e = input.h only.",
   "commit": "482324e",
   "depends_on": [
    105,
    154
   ],
   "implementation": "src/input.h:131-136 additive widened name block after the Step 105 table. tests/hostile_data_test.cpp: new checkKeyNames() (~3628-3692, ~50 named-key assertions) wired into main() as keyNamesOk; checkInputBindings MoveLeft=A,LEFT,TAB end-to-end widened-name bind proof.",
   "verification": "On committed HEAD (482324e + 60ac7d6): cmake --build build --config Release -> exit 0, zero errors; ctest --test-dir build -C Release -> 1/1 Passed 3.24 s; PureEngine/Platformer/Pong alive True (4 s probes). NOT verified: interactive feel of rebinding to a widened key (no keyboard injection into games), per increment scope."
  },
  {
   "id": 156,
   "name": "Scene/prefab/persistence test campaign (manager save, spawn-after-kill, multi-spawn persist)",
   "status": "completed",
   "definition_of_done": "Three headless tests close the scene/prefab/persistence coverage gaps: checkSceneManagerSave (first-ever coverage of saveSceneManagerToFile: index file + per-scene files + round-trip + empty-name refuse), checkSpawnAfterKill (append semantics, no slot reuse, index stability, safe no-op kills), checkPrefabMultiSpawnPersist (2 live + 1 killed -> save -> load -> count 2, positions in order, configs intact, source unmutated). 84 test-case functions total. No engine code.",
   "notes": "Sweep-in honesty: checkSceneManagerSave was committed inside physics c1919f6 (shared-tree collision - the physics session commit swept the shared test file while the edits were in the working tree; disclosed when discovered, my own commit found nothing staged). Increments 2/3 committed directly (842214e, 60ac7d6). Two first-run test failures owned and fixed (inverted alive check; spawn-order assumption). Campaign recorded at the next free id 156 after the input session landed its Step 155 record (b5ce83d) - racing 155/156 would have risked a duplicate.",
   "commit": "60ac7d6",
   "depends_on": [
    136,
    154
   ],
   "implementation": "tests/hostile_data_test.cpp:3774 checkSceneManagerSave (2-scene manager alpha/beta, rmArtifacts 3-prefix cleanup, index-file content asserts, per-scene round-trips via loadSceneFromFile, empty-name refuse, assets/ pre-created); :3875 checkSpawnAfterKill (spawn->kill->spawn, append at new index, dead slot stays dead, slot-0 stable, out-of-range/double kill no-ops); :3937 checkPrefabMultiSpawnPersist (2 live prefab entities + killed -> save -> load -> count 2, per-entity positions/configs in order, source unmutated); registered at :4255-4257 + gated.",
   "verification": "cmake --build build --config Release -> zero errors (all 7 targets); ctest --test-dir build -C Release -> 1/1 Passed (1.09 s final run; each increment verified: 2.12 s / 9.82 s post-fix / 2.55 s post-fix); direct binary runs zero failure messages; arcade/platformer/pong alive True after each increment. No engine feature work."
  },
  {
   "id": 157,
   "name": "Input: keysForAllActions() union helper (rebind-safe edge tracking)",
   "status": "completed",
   "definition_of_done": "keysForAllActions() pure helper: deduped union (first-seen order) of every key across all 8 Actions, defaults + overrides, no GLFW/file I/O. checkKeysForAllActions test: default union is 12 unique keys (shared W/UP, SPACE, ESCAPE deduped); after a file remap to a widened key (Jump=F1) the union gains F1 while shared SPACE stays and dedup holds; resetActionOverrides round-trips to the defaults; shipped-file state restored at test end. Games unchanged — adoption documented later.",
   "notes": "Input-campaign increment 3 (session PureEngine_input). Purpose: close the silent untracked-key gap — isActionEdge only fires for keys registered at Input construction, so a game constructing Input with keysForAllActions() keeps firing after any loadInputBindings remap. Step-id note: code comments say Step 157 because the scene/prefab campaign claimed 156 while this increment was in flight. Committed directly this time (2d7be33) — the tree was clean and the shared test file contained only input hunks.",
   "commit": "2d7be33",
   "depends_on": [
    105,
    154,
    155
   ],
   "implementation": "src/input.h:99-121 keysForAllActions() (static Action list, nested loops, dedup via membership scan — no <algorithm>, matching file style). tests/hostile_data_test.cpp:3695 checkKeysForAllActions() (defaults = 12 unique; tmp file Jump=F1 -> union 13, F1 present, shared SPACE kept; dup scan in all states; reset round-trip; trailing loadInputBindings(input_bindings.txt) restore), wired as keysAllOk at :4319. No game call-site changes — arcade/platformer could construct Input with keysForAllActions() to be rebind-proof; left as documented adoption.",
   "verification": "cmake --build build --config Release -> exit 0, Select-String 'error' no matches; ctest --test-dir build -C Release -> 1/1 Passed 5.00 s (fresh binary, includes checkKeysForAllActions); PureEngine/Platformer/Pong alive True (4 s probes from D:\\PureEngine). NOT verified: interactive rebind-to-previously-untracked-key feel in a live game (helper is game-adopted nowhere yet), per increment scope."
  },
  {
   "id": 158,
   "name": "Input: gamepad-action bridge (fixed mapping + optional pad tails)",
   "status": "completed",
   "definition_of_done": "gamepadButtonsForAction(Action) fixed mapping (MoveLeft/Right/Up/Down -> d-pad, Jump/Confirm -> A, Pause -> START, Back -> B), deliberately NOT actionOverrides (key codes and button ids share numeric ranges — reusing the table would conflate namespaces). Input::isActionDown/Edge gain optional GamepadState tails (default null = keyboard-only, byte-identical call sites); with pads passed an action fires from either device. gamepad.h untouched; no game call-site changes.",
   "notes": "Input-campaign increment 4 (session PureEngine_input). Test-file rule: tests/hostile_data_test.cpp NOT modified this step (physics session owned it; P6 landed 73883ef during this increment). Bridge helpers are regression-verified only (CTest green on untouched tests) — no headless pad-state tests this commit, deferred to a later step when the shared test file frees up. Games may keep hand-OR patterns (unchanged); optional one-line adoption left for later. Design note: no connected gate on the pad tail, matching gamepadButton's no-gate precedent.",
   "commit": "d5c40cc",
   "depends_on": [
    69,
    88,
    157
   ],
   "implementation": "src/input.h:54 #include \"gamepad.h\" (after the glfw3.h include — GLFW_INCLUDE_NONE behavior unchanged in every TU). :123-141 gamepadButtonsForAction(Action) fixed switch table. :319-351 isActionDown/isActionEdge optional GamepadState tails (const pointers, default nullptr; pad tail loops gamepadButtonsForAction with gamepadButton/gamepadButtonEdge — the existing pure gamepad reads, no new edge logic).",
   "verification": "cmake --build build --config Release -> exit 0, Select-String 'error' no matches in inc4_build.log; ctest --test-dir build -C Release -> 1/1 Passed 7.52 s (hostile_data_test untouched — regression proof); PureEngine/Platformer/Pong alive True (4 s probes from D:\\PureEngine). NOT verified: headless pad-state tests for the bridge (test file frozen to physics this step); hardware gamepad feel (no pad attached) — explicitly not claimed."
  },
  {
   "id": 159,
   "name": "Input: Action::Console (rebindable console toggle)",
   "status": "completed",
   "definition_of_done": "Action::Console added (enum value after Back, default mapping {GRAVE_ACCENT}); loadInputBindings maps the console action name to it (was an odd Back alias that let the shipped Console=GRAVE line clobber Back keys — latent bug fixed; Back action unused in games, no gameplay impact). Thin call sites: arcade pumpConsole (main.cpp) and platformer pumpConsole toggle via isActionEdge(Action::Console) — identical behavior with shipped bindings, rebindable via file. keysForAllActions unchanged at 8 actions (Console excluded until the shared-file union test can assert the new default; documented in code).",
   "notes": "Input-campaign increment 5 (session PureEngine_input) — final increment; campaign complete (Steps 154-159). Test-file rule: tests/hostile_data_test.cpp NOT modified (frozen); regression-verified only. GRAVE was already edge-tracked in both games (main.cpp:831, platformer.cpp:224), so the isActionEdge swap is semantics-identical. First commit attempt failed (escaped quotes mangled by the shell split the message into pathspecs; nothing committed); retried with a quote-free message successfully.",
   "commit": "7428d77",
   "depends_on": [
    105,
    154,
    158
   ],
   "implementation": "src/input.h:78-87 Action enum + Console; :98 case Action::Console default {GRAVE_ACCENT}; :211 loader console -> Action::Console; :107-117 keysForAllActions Step 159 exclusion note. main.cpp:1351 and games/platformer/platformer.cpp:279 pumpConsole toggles via isActionEdge(Action::Console).",
   "verification": "cmake --build build --config Release -> exit 0, Select-String 'error' no matches in inc5_build.log; ctest --test-dir build -C Release -> 1/1 Passed 18.06 s (includes the frozen union test asserting defaults == 12 — confirms Console correctly excluded from keysForAllActions, and the shipped-file restore path mapping Console=GRAVE properly); PureEngine/Platformer/Pong alive True (4 s probes from D:\\PureEngine). NOT verified: interactive console-toggle rebind (remap Console=SPACE in a file and play) — deferred with the rest of the campaign's interactive-feel items."
  },
  {
   "id": 160,
   "name": "Input: keysForAllActions union includes Action::Console + games adoption",
   "status": "completed",
   "definition_of_done": "keysForAllActions() union includes Action::Console (default GRAVE was the only key missing; union default 12->13 keys, remap test 13->14); gamepad-action bridge adopted in games Jump+Pause. Input campaign Steps 154-159 + this follow-up complete.",
   "notes": "Adoption commits 3e51b8f (platformer.cpp/main.cpp) and 8bfd7e8 (tests) carry duplicate Step 160 messages (tangle, left as-is per no-rewrite ruling). Union follow-up commit 2cacf5f landed via split-stage: only union hunks staged; scene session in-flight round-trip test hunks left unstaged.",
   "commit": "2cacf5f",
   "depends_on": [
    159
   ],
   "implementation": "src/input.h keysForAllActions all[] += Action::Console; tests checkKeysForAllActions asserts 13-key default + GRAVE_ACCENT, remap assert 13->14.",
   "verification": "Build zero errors; checkKeysForAllActions PASSED (union 13, no dups; remap gains F1, SPACE stays); arcade/platformer/pong alive True. Working-tree CTest fails ONLY in scene session in-flight checkSceneManagerRoundTrip (round-trip beta tilemap mismatch) — test does not exist in committed state, unrelated to this step."
  },
  {
   "id": 161,
   "name": "Scene: loadSceneManagerToFile + manager round-trip",
   "status": "completed",
   "definition_of_done": "loadSceneManagerFromFile (v1/v2 manager parse, unknown-version guard) + checkSceneManagerRoundTrip test: save -> load -> identical state. Manager-save path symmetry now tested end to end.",
   "notes": "Split-stage not needed: test file held scene-only hunks (3 hunks: new test + registration + assertion chain). Owner-staged platformer.cpp temporarily unstaged and restored after commit.",
   "commit": "c8e40fc",
   "depends_on": [
    156
   ],
   "implementation": "src/scene.h loadSceneManagerFromFile +83; tests checkSceneManagerRoundTrip (round-trip beta tilemap/cells mismatch guards fixed by scene session, verified green before commit).",
   "verification": "Fresh sanity BEFORE commit: build zero errors, ctest 1/1 Passed 2.28s (previous failure round-trip beta tilemap mismatch fixed by scene session). Arcade/platformer/pong alive re-check pending push; committed-tree green by build+ctest on identical worktree."
  },
  {
   "id": 162,
   "name": "Scene: manager save path symmetry + Windows re-save rename fix",
   "status": "completed",
   "definition_of_done": "saveSceneManagerToFile honors an explicit path (path symmetry with saveSceneToFile); fs::rename re-save fix in both saveSceneToFile and saveSceneManagerToFile (Windows cannot rename onto an existing file — re-save clobbered the target); test case 5e covers explicit-path save + .tmp cleanup.",
   "notes": "Scene campaign complete: load (161) + path symmetry + re-save (162). Fresh sanity green before commit (ctest 1/1 Passed 1.46s) after prior in-flight failure (round-trip: explicit-path save failed) was fixed by scene session.",
   "commit": "a1824af",
   "depends_on": [
    161
   ],
   "implementation": "src/scene.h saveSceneToFile/saveSceneManagerToFile explicit-path + rename-overwrite handling (+47); tests case 5e explicit-path save (+34).",
   "verification": "Build zero errors; ctest 1/1 Passed 1.46s before commit; post-commit sanity + alive pending push."
  },
  {
   "id": 163,
   "name": "Resources: load system contract test (failure-not-cached, copy independence, pack semantics)",
   "status": "completed",
   "definition_of_done": "checkResourceSystem headless contract test closes the gaps checkBinaryBlob (Step 101) left open: failed blob loads leave out empty + full-read prefix match on a committed asset, cache holds exactly the loaded entries (failure never cached, retry possible), returned copies are independent of the cache, pack v1 hit = pack file bytes, pack fallback = entryName direct file, both-missing = false, clearBinaryCache empties the map.",
   "notes": "Contract defined and verified — engine code unchanged (the boundary already holds; gaps were test gaps only). Texture load paths need GL and stay covered by the Step 133 renderer test + in-game loads; 3-candidate CWD probe order guaranteed by construction (shared literal candidate list).",
   "commit": "bf45f1b",
   "depends_on": [
    101,
    133
   ],
   "implementation": "tests/hostile_data_test.cpp checkResourceSystem (+67) + explicit resources.h include; registered in main() + failure chain.",
   "verification": "Build zero errors; ctest 1/1 Passed 9.06s (107 test-case functions); arcade/platformer/pong alive True."
  },
  {
   "id": 164,
   "name": "Animation system contract (load/bind/update/switch/loop/end)",
   "status": "completed",
   "definition_of_done": "checkAnimationSystem covers: valid parse, malformed skip, duplicate names, missing file, bind/switch/loop/non-loop/zero-duration/speed-freeze. No engine source changes.",
   "implementation": "tests/hostile_data_test.cpp: checkAnimationSystem cases 1-12",
   "verification": "cmake->EXIT=0; ctest 1/1 Passed 2.15s (report) — re-verified at commit: 2/2 Passed 1.72s (new resources_test target from concurrent session); PureEngine/Platformer/Pong alive",
   "commit": "2e11c78",
   "depends_on": [
    56,
    58
   ]
  },
  {
   "id": 165,
   "name": "Camera followLerp contract (blend/convergence/overshoot/clamp)",
   "status": "completed",
   "definition_of_done": "checkFollowLerp: partial approach, convergence, upper clamp (huge dt), lower clamp (neg dt), z unchanged, additive with follow(). No src changes.",
   "implementation": "tests/hostile_data_test.cpp: checkFollowLerp +70",
   "verification": "cmake->EXIT=0; ctest 2/2 Passed (report 1/1 2.72s; new resources_test target landed from concurrent session); all games alive — alive re-check at final verify",
   "commit": "feb7db3",
   "depends_on": [
    73
   ],
   "notes": "Split-stage: events session in-flight checkEventReentrantOnceGaps hunks (+105) + eventGapOk registration/chain left unstaged (staging the chain would reference their undefined test; rewriting their chain line would drop my token on their commit). followLerpOk registered but NOT yet in the failure chain — chain-gating lands with the events commit or a later pass (chain line owned by events in-flight edit)."
  },
  {
   "id": 166,
   "name": "Resources system contract (probe/failure/cache/ownership + resources_test)",
   "status": "completed",
   "definition_of_done": "CONTRACT block in resources.h. resources_test covers: blob failure/miss/dir, probe priority, texture all-miss (no GL), cache semantics, pack hit/fallback. 2/2 CTest passed.",
   "implementation": "src/resources.h:41 CONTRACT block. tests/resources_test.cpp new. CMakeLists.txt resources_test target.",
   "verification": "cmake->EXIT=0; ctest 2/2 Passed; PureEngine alive",
   "commit": "4df2842",
   "depends_on": [
    14
   ]
  },
  {
   "id": 167,
   "name": "Events contract gaps (reentrant emit, once auto-removal, throw isolation)",
   "status": "completed",
   "definition_of_done": "checkEventReentrantOnceGaps: reentrant depth-3 snapshot, mid-dispatch removal, once refusals, once auto-removal, once+throw. followLerpOk chain gate fixed. 2/2 CTest.",
   "implementation": "tests/hostile_data_test.cpp: checkEventReentrantOnceGaps +~110 lines",
   "verification": "cmake->EXIT=0; ctest 2/2 Passed 6.74s; all games alive — alive at final verify",
   "commit": "e4b9bbf",
   "depends_on": [
    91
   ]
  },
  {
   "id": 168,
   "name": "Console contract gaps (wrap cap, empty-history recall, submitHistory-vs-clear, closed-console no-op)",
   "status": "completed",
   "definition_of_done": "checkConsoleContract closes the halves the five checkConsole* tests left open: Step 95 wrap splits long lines to 38-col pieces with the 64-line cap enforced under wrapped submits, Up/Down recall is a no-op on empty submitHistory, submitHistory survives clear (lines wiped only), a new submit resets historyPos to the end, drawConsole on a CLOSED console with empty lines is a no-op making zero renderer calls (headless-safe).",
   "notes": "First build FAILED: LNK2019 glad_gl* unresolved in drawTextString — the console contract test references drawConsole -> drawTextString -> glad symbols; fixed by compiling the already-fetched glad_gl.c into hostile_data_test (same pattern as resources_test; closed-console path never CALLS the GL pointers). Registration idempotency/unknown-command/feedKey/history-cap already covered by existing tests.",
   "commit": "2d2bdcf",
   "depends_on": [
    68,
    95,
    144
   ]
  },
  {
   "id": 169,
   "name": "Particles system contract (headless lock)",
   "status": "completed",
   "definition_of_done": "checkParticleContract: non-positive maxParticles never spawns, accumulator carry + dt<=0 untouched, maxLife survives update, negative-life never converts, empty-pool conversion, swap-with-back reorder. No src changes.",
   "implementation": "tests/hostile_data_test.cpp: checkParticleContract (+125) + registration + failure chain; render row note on board.",
   "verification": "Fresh sanity before commit: cmake->EXIT=0; ctest 2/2 Passed 4.16s/0.91s; alive at post-commit check pending.",
   "commit": "67cc2c2",
   "depends_on": [
    57
   ]
  },
  {
   "id": 170,
   "name": "Time/timestep contract (tick advance, clamp observed, pause/scale semantics)",
   "status": "completed",
   "definition_of_done": "checkTimeContract: tick advances per frame (deltas never cumulative), the 0.1s clamp engages on a real long frame (Sleep 150ms -> exactly 0.1f), paused scaledTick returns 0 while the clock still advances underneath, scaledTick = scale*delta, scale 0 gives 0 unpaused, defaults restored. No time.h changes.",
   "notes": "First ctest run FAILED: tick returned 0 for every call — glfwGetTime() requires glfwInit() and checkInputEdges terminates GLFW before this test runs; fixed by self-containing glfwInit/glfwTerminate inside checkTimeContract (headless init, no window). Gap-closing only: delta clamp, timeScale, pause/scaledTick, non-negative monotonic dt.",
   "commit": "682f91b",
   "depends_on": [
    17,
    92
   ]
  }
 ],
 "kill_criteria": "If any step's scope keeps expanding instead of shrinking, stop, cut scope, and re-record a smaller definition_of_done before continuing. Do not introduce an abstraction, manager, registry, or subsystem unless the current implementation demonstrates a concrete need for it."
}