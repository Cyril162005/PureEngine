{
  "project": "PureEngine",
  "method": "from_scratch",
  "hardware_target": "i3, 8GB RAM",
  "stack": ["C/C++", "GLFW or SDL", "OpenGL"],
  "steps": [
    {
      "id": 1,
      "name": "Window + Context Creation",
      "status": "completed",
      "definition_of_done": "Blank window opens, stays open, closes without crash",
      "notes": "Fixed missing GL loader (GLAD) and clear/swap calls. Verified black window.",
      "depends_on": []
    },
    {
      "id": 2,
      "name": "Render Loop",
      "status": "completed",
      "definition_of_done": "Clears screen every frame, tracks delta time, closes cleanly",
      "notes": "Added delta time tracking using glfwGetTime(); verified black window updates every frame and closes cleanly.",
      "depends_on": [1]
    },
    {
      "id": 3,
      "name": "Input Handling",
      "status": "completed",
      "definition_of_done": "Key press visibly changes something on screen",
      "notes": "Keyboard polled with glfwGetKey() each frame; ESC closes window, SPACE toggles clear color black/dark blue via edge detection (one toggle per press). Verified: no flicker on hold, ESC closes cleanly.",
      "depends_on": [2]
    },
    {
      "id": 4,
      "name": "Draw One Shape",
      "status": "completed",
      "definition_of_done": "Triangle or quad renders via a basic shader",
      "notes": "Triangle drawn via inline GLSL shader program (compile/link error-checked) with VAO/VBO vertex setup and glDrawArrays each frame. Verified: orange triangle centered on black; ESC/SPACE still work.",
      "depends_on": [2, 3]
    },
    {
      "id": 5,
      "name": "Math Layer",
      "status": "completed",
      "definition_of_done": "Can translate/rotate/scale shape using own or understood math code",
      "notes": "Own header-only math layer, no external library: Vec3 (dot/cross/length/normalized/operators) and column-major Mat4 (identity/translation/scale/rotationZ, multiply, transformPoint) in src/math/. Uniform 'transform' uploaded per frame via glUniformMatrix4fv; rotation driven by deltaTime at 0.9 rad/s. Compile-time verified with static_asserts; visually verified: counter-clockwise rotation ~7s per revolution, ESC/SPACE intact.",
      "depends_on": [4]
    },
    {
      "id": 6,
      "name": "Sprite/Mesh Rendering + Camera",
      "status": "completed",
      "definition_of_done": "Assets rendered in space, camera can move/view them",
      "notes": "Own math layer extended with orthographic() (constexpr, static_assert-proven) and lookAt() (view = inverse camera transform built from orthonormal axes). MVP = projection * view * model uploaded per instance via glUniformMatrix4fv; two triangle instances at world (-1.5,0) and (+1.5,0) from identical vertex data, spinning around own centers. WASD pans camera 3 units/s via deltaTime. Visually verified: independent spins, correctly inverted camera pan, ESC/SPACE intact.",
      "depends_on": [5]
    },
    {
      "id": 7,
      "name": "Basic Object/ECS System",
      "status": "completed",
      "definition_of_done": "Game entities represented without hardcoding each one",
      "notes": "Simple array-of-structs, not full ECS (design ruling: no payoff at this scale). Header-only src/entity.h: pe::Entity (position/rotationAngle/rotationSpeed/scale) with update(dt) and modelMatrix() = translation * rotationZ * scale. std::vector<pe::Entity> with one update loop and one draw loop replaced hardcoded model1/model2 blocks; third instance (clockwise -1.4 rad/s, 0.6 scale) added as pure data. Visually verified: independent per-entity spins, WASD/SPACE/ESC intact.",
      "depends_on": [6]
    },
    {
      "id": 8,
      "name": "Collision Detection (AABB)",
      "status": "completed",
      "definition_of_done": "Two objects can be detected as overlapping",
      "notes": "AABB chosen over circles/exact tests; halfExtents added directly to Entity (0.7071 rotation-safe bound, multiplied by scale at test time). New header-only src/collision.h: constexpr aabbOverlap (separating-axis, AND on both axes) proven by 3 static_asserts. Arrow keys move entities[0] at 2.5 units/s (WASD stays camera); per-frame derived collision flags (unique pairs, j=i+1); fragment shader gained uniform vec3 color — colliding entities render red. Visually verified: both entities red on overlap, instant clear on separation, all prior inputs intact.",
      "depends_on": [7]
    },
    {
      "id": 9,
      "name": "Audio Playback",
      "status": "not_started",
      "definition_of_done": "A sound file loads and plays on trigger",
      "depends_on": [2]
    },
    {
      "id": 10,
      "name": "Asset Loading",
      "status": "not_started",
      "definition_of_done": "Textures/models load from files, not hardcoded",
      "depends_on": [6]
    },
    {
      "id": 11,
      "name": "Scene/Level Structure",
      "status": "not_started",
      "definition_of_done": "Can load, hold, and switch between distinct game states",
      "depends_on": [7, 10]
    },
    {
      "id": 12,
      "name": "Game Logic Layer",
      "status": "not_started",
      "definition_of_done": "Franchise-specific gameplay built on top of engine systems",
      "depends_on": [8, 9, 11]
    }
  ],
  "kill_criteria": "Stuck 2+ weeks on one step with no forward progress -> stop, defend/fix/kill before continuing"
}